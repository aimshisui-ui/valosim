# VALOSIM FULL-CODEBASE AUDIT — INSIGHTS REPORT
Date: 2026-07-02
Method: 16-agent workflow (10 subsystem readers covering all ~65k lines of cpp/src, cpp/include, ui-poc/app.html -> 6 specialist insight agents), followed by an 18-agent adversarial verification pass on the hard factual claims. ~2.6M agent tokens, 412 tool uses total.
Verification outcome: 16/18 claims CONFIRMED, 2 partially true (corrections noted in the verification appendix — 'expiring-invisible': the player modal DOES show an Expires label at app.html:7281, but nothing anywhere warns/highlights expiring deals; 'market-quicksign': the one-click-full-ask-no-confirm part is confirmed, but a refused quick offer does NOT durably raise the ask — register_rejected_offer at Player.cpp:3515-3532 is a decaying mood hit that no-ops at ratio ~1.0).

Categories: ui-changes (16) · features (18) · qol (18) · visual-polish (20) · flavor-text (16) · consistency (18) = 106 findings.

# CATEGORY: ui-changes

## [1] Collapse the Transfers/Recruitment/Scouting triple-split into one hub; delete the redundant Market screen  {prio=high effort=medium}
WHERE: ui-poc/app.html:2403-2405, 4936-5072, 5617-5648
The nav rail ships three separate acquisition screens (Transfers→market, Recruitment, Scouting at app.html:2403-2405), and Market is a strict subset of Recruitment: its FA table (renderMarket, app.html:4962-5000) is capped at 40 rows with no sort or filter, while Recruitment's table over the full world pool already has sortable headers (RC_COLS/rcRenderHead app.html:5447-5535) and an 'FA' availability filter (rcFiltered app.html:5486). The staff-hire card is fully duplicated too (app.html:5002-5063 vs rcRenderStaff app.html:5617-5648). Repoint navMkt at Recruitment's Free Agents tab, fold the Scouting screen's knowledge map + assignments into Recruitment's Assignments tab (it already hosts briefs), and retire renderMarket — one 'Transfers' destination like FM's Transfer Centre instead of three half-overlapping ones.

## [2] Market's one-click Sign silently commits at full ask — route it through the negotiation UI that already exists  {prio=high effort=small}
WHERE: ui-poc/app.html:4927, 4992-4994, 6938, 7148-7151
marketSignFA posts act:signfa:<id>:market (app.html:4927), the host's 4-token quick-sign path that pays the listed ask with default terms — no years, role, or bonus choice, and no chance to sign below an inflated mood-ask. Yet the player modal already has a full Sign tab with a live acceptance verdict, sliders, and counter-offers (canSign gating app.html:7202, Sign-jump button app.html:7148-7151). Change the FA row's Sign button to open the player modal directly on the Sign tab (openPlayerModal at app.html:6938 currently takes only an id — add a tab parameter that presets pmTab). This turns every signing into a real decision and removes a trap where the best UI in the game is hidden behind a tiny profile arrow.

## [3] Contract expiries are invisible — add an expiring-deals surface with a Re-sign quick action  {prio=high effort=medium}
WHERE: ui-poc/app.html:3020-3035, 3523-3533
Grep finds zero occurrences of 'expir' in app.html. The engine's biggest offseason drama (AI re-sign pass at years_left<=1, walked stars drawing rival bids in B8) is driven by contract runway, but the UI shows it only as a plain '1y · $95K' cell in the roster (app.html:3035) and an unhighlighted YRS column in Finance (app.html:3523-3533). Add: red/amber tint on years_left<=1 rows in both tables, an 'EXPIRING' count KPI on the Squad screen, and a per-row 'Re-sign' button that opens the player modal preset to its Re-sign tab (same openPlayerModal tab param as the Sign fix). Without this, players discover expiries when the 'walked to a rival' mail arrives.

## [4] Standings tables have no playoff/relegation zone markers, and the dashboard legend states the wrong playoff cut  {prio=high effort=small}
WHERE: ui-poc/app.html:3374-3401, 2534-2536
The full Competition standings render RK/TEAM/W-L/OVR/FORM with no zone bands at all (app.html:3374-3401), even though the engine's regional playoffs take the top 8 by phase_wins (GameManager.cpp:620-651) and tier-1 bottom-3 enters the relegation gauntlet (GameManager.cpp:6577-6596). Meanwhile the dashboard mini-table marks top-4 green with a legend reading 'Playoff line · top 4' (app.html:2732-2735, 2535) — the wrong number — and its footer 'Stage 1 · Rd 9/12' is hardcoded static text never updated by renderDashboard (app.html:2536). Add hairline separator rows after 8th and above the bottom-3 (tier-aware) in Competition, correct the dashboard legend to top-8, and bind the footer to header.current_phase which every payload already carries.

## [5] Matchday decisions are scattered across three screens — build one pre-match panel  {prio=high effort=medium}
WHERE: ui-poc/app.html:2743-2791, 4676-4724
Team talk chips exist only in the dashboard hero and only when days_until===0 (app.html:2774-2787); the analyst's opponent intel (est. OVR ± band, key players, recommended counter) lives on Strategy (app.html:4676-4724); map prep/comp overrides are also on Strategy; and ENTER MATCHDAY fires from the dashboard (app.html:2790). Reading the scout report means leaving the screen where you set the talk and start the match — and the dashboard's win-prob tape is a client-side fabrication (mePct=50+ovrDiff*6, app.html:2765) while the real analyst estimate sits one screen away. On matchday, render a combined pre-match block (opponent intel summary + team talk chips + prep status + Enter button) either by extending the hero or by giving Strategy the talk chips and a matchday CTA; replace the fake tape with opp.ovr_est/ovr_band.

## [6] No player-vs-player comparison view anywhere  {prio=medium effort=medium}
WHERE: ui-poc/app.html:7091-7221 (modal), 5476-5495 (shortlist tab)
Every upgrade decision ('is this 74-OVR FA Sentinel better than my starter?') requires memorizing attribute bars between modal opens — the FM-staple side-by-side is absent. The engine already exports full attribute arrays, role_fits, contract and mood for any player via nav:playermodal, so no engine work is needed for a client-side compare. Minimum viable: in the modal's Sign/Bid tabs, show your current starter of the same role (name, OVR, rating, wage) inline next to the candidate. Full version: a 'Compare' pin in the modal header that holds player A and renders delta-colored attribute bars when player B opens, or a compare tray on the Recruitment shortlist tab.

## [7] Roster table: no column sorting, and morale/discontent invisible until WANTS OUT  {prio=medium effort=small}
WHERE: ui-poc/app.html:3008-3045, 5524-5535
The squad table header is static spans with no sort handlers (app.html:3008) while Recruitment proves the sortable-header pattern exists in this codebase (rcRenderHead app.html:5524-5535) — reuse it. Worse, the exporter sends per-player discontent (DashboardExport.cpp:719) and the UI never reads it (zero grep hits); the only unhappiness signal is the WANTS OUT badge (app.html:3018), which appears at discontent>=0.6 — after the transfer request is filed. Add a sortable MOR/mood column (mood already drives the detail-panel meter at app.html:3134) so brewing grievances are visible while a new deal or starter promise can still fix them.

## [8] Bench/Start buttons hardcode the swap partner — the manager can't pick who comes out  {prio=medium effort=small}
WHERE: ui-poc/app.html:3009-3025
renderRoster precomputes a single topBench and weakestStarter and every Bench/Start button can only swap with those two (app.html:3009-3025), even though the bridge verb act:bench:<a>:<b> accepts arbitrary ids. Benching your slumping Duelist for the bench Duelist (instead of the highest-OVR bench Controller) is impossible without god mode. Redesign: clicking Bench puts the table in 'choose replacement' mode highlighting bench rows (click one to post the pair), or add a small per-row select — this is a lineup-control basic for a management sim and the backend already supports it.

## [9] Mail inbox: no category/unread/starred filters and no unread badge on the nav rail  {prio=medium effort=medium}
WHERE: ui-poc/app.html:4465-4490, 2406
Every mail item carries category, read, and important flags (app.html:4471-4479) and MAIL_COLOR already enumerates the categories, but the inbox is one flat 40-item list — separating board objectives from media noise means scrolling. Add a client-side filter pill row (All / Unread / Starred / per-category) above the list; pure JS over the existing items array. Separately, the nav Mail button (app.html:2406) shows no unread indicator, so mail that gates decisions (transfer requests, board warnings) goes unnoticed for days; the host would need to add the unread count to the shared header object every exporter already emits, then paint a dot on #navMail.

## [10] Team Profile is data-starved: no trophy case, placements, or identity blurb  {prio=medium effort=medium}
WHERE: ui-poc/app.html:6127-6241; cpp/src/DashboardExport.cpp:2008-2017
The history card renders only 'Y3 · Regular season · 12-2' rows (app.html:6233-6238) and the identity chips stop at Prestige/Brand/Wealth/Record/OVR (app.html:6129-6135). The engine tracks exactly what a rival-club page needs — Team::trophy_case, dynasty_tier, finals appearances, strategy name+blurb, top_chemistry_pairs — but export_team_profile ships none of it (DashboardExport.cpp:2008-2017 emits year/wins/losses only). Add a trophy ribbon and per-season placement to the history rows and a strategy/dynasty chip to tpIdentity; small exporter additions, existing UI slots. Right now scouting a bracket opponent's pedigree is impossible in-game.

## [11] Scouting/watchlist deep links open the player modal on the wrong tab  {prio=medium effort=small}
WHERE: ui-poc/app.html:5282, 5307, 6938, 7205-7208
Completed scout-report rows and in-progress assignment cards on the Scouting screen call openPlayerModal(id) (app.html:5282, 5307), but the modal restores whatever pmTab was last used (app.html:7205-7208) — clicking a finished report usually lands on Overview, not the report you clicked. Extend openPlayerModal(id, tab) to preset pmTab, then: Scouting rows pass 'scout', mail 'View player' on contract mail could pass 'resign', and the watchlist could pass 'bid'. One tiny function change unlocks correct deep-linking from four screens.

## [12] Interest and Scout panes serve stale cached data for the whole session  {prio=medium effort=small}
WHERE: ui-poc/app.html:6617-6622, 7111, 8023-8031
pmInterestAsked/pmScoutAsked reset only when a different player id renders (app.html:7111), and pmInterestCache/pmScoutCache are never invalidated — re-opening the same player after advancing weeks repaints the market scan and fog-band from weeks ago, quietly misinforming transfer decisions. Add a 'Rescan' refresh button in each pane header (re-fires get:playerinterest/get:scoutreport) and clear both caches whenever advance() runs, so a day passing invalidates market intel like it should.

## [13] Watchlist 'Bidding' status is a label with no bid action  {prio=medium effort=small}
WHERE: ui-poc/app.html:3711-3736
The watchlist row's status select offers Watching/Scouting/Bidding (app.html:3717, 3726) but setting Bidding does nothing — there is no path from the stated intent to the act:buyplayer flow, and the row shows no fee/ask column to act on. When status==2, render a trailing 'Make bid →' button that opens the player modal on the Bid tab (free with the openPlayerModal tab param), and add a Value/Ask column so the watchlist works as the shortlist-to-action pipeline it pretends to be.

## [14] Brackets screen is empty for most of the season — add a completed-events recap  {prio=medium effort=medium}
WHERE: ui-poc/app.html:5908-6034; cpp/src/DashboardExport.cpp:1823-1898
active_tournaments is cleared at every phase boundary, so during STAGE phases (the majority of the 200-day year) the Brackets nav shows only 'No playoffs in progress — brackets appear during Regionals, Masters & Champions.' (app.html:5936). The season's finished events — who won Regionals 1, your Masters run — vanish from the UI entirely (they survive only as news items and player award strings). Under the empty state, render a 'This season' list of completed events with champion, runner-up, and the user's finish; needs a small season_results array in export_brackets (the engine already records trophies, finals appearances, and qualifiers at tournament finish, GameManager.cpp:948-993).

## [15] God-mode ±1 steppers are rate-limited to one point per host round-trip — use direct numeric input  {prio=low effort=small}
WHERE: ui-poc/app.html:3200-3223, 2994-2997
Attribute/potential steppers compute base±1 from the render-time value, so rapid clicks before the host re-push no-op (app.html:3200-3223; same pattern in the modal at 7946+). Dragging an attribute from 55 to 90 takes 35 full state round-trips. act:setattr already accepts any clamped 1-99 value, and the club god-editor already uses type-a-number-and-Apply inputs (app.html:2994-2997) — replace the steppers with the same numeric input (commit on blur/Enter) for attributes and potential in both the roster detail panel and the player modal.

## [16] Live viewer kill feed ignores exported alive counts — free clutch context  {prio=low effort=small}
WHERE: ui-poc/app.html:4251-4394; cpp/src/DashboardExport.cpp:3777-3778
The match exporter ships t1_alive/t2_alive with every kill event (DashboardExport.cpp:3777-3778) but app.html never reads them (zero grep hits). The round-by-round detail could tag each kill with the resulting man-count ('3v5') and badge clutch situations (1vN entered/won) at render time with no engine change — exactly the drama the live viewer exists to sell. Add the count to each kill-feed line in mvBuildMapReplay and a small 'CLUTCH' chip when a team's alive count hits 1 while the round is still live.

# CATEGORY: features

## [1] Incoming transfer offers for your players (sell-side market)  {prio=high effort=medium}
WHERE: GameManager.cpp:1965 (mail copy), DashboardExport.cpp:3490-3666 (interest export), Team.cpp:2982 (fee pipeline)
The transfer-request mail literally tells the player to 'listen to bids from rivals' (GameManager.cpp:1965), but no AI club ever bids on a user-rostered player — the only involuntary exits are silent forced 'restless star' departures (cpp:2669-2673). The machinery already exists: Team::score_scout_target powers the Interest tab's 'pursuing clubs' list (DashboardExport.cpp:3550), so those same clubs should periodically convert interest into a real offer at Team::transfer_fee_for value. Deliver as a mail with accept/decline via a new act: verb; accept runs the existing release_player→pay_transfer_fee→sign_player pipeline (Team.cpp:2982-3006), decline bumps the player's discontent (Player.h grievance fields). This closes the biggest missing half of the transfer market and makes the Interest tab consequential instead of decorative.

## [2] Map pick/ban veto phase before every series  {prio=high effort=medium}
WHERE: GameManager.cpp:706-713, Tournament.cpp:484-493, Team.cpp:1554-1676 (decide_desired_tag as scoring precedent)
Every series driver draws each map uniformly at random from maps() — repeats possible within one BO3 (GameManager.cpp:706-708, Tournament.cpp:485). A VCT-style veto (ban-ban-pick-pick-ban-ban-decider) is the single most authentic missing VALORANT mechanic and gives the user's map_prep levels (Team.h:401-402) and per-player map mastery (Player.cpp:805-833) a strategic payoff. AI veto choices should be RNG-free deterministic scoring from Team::map_pref + roster map mastery so AI-vs-AI sims keep the dynasty RNG stream byte-identical; the user gets an interactive veto panel on the existing pre-match screen (which already hosts team talk + the analyst report, GameManager.cpp:4230-4299). Replace the rng().irange map draw in sim_series_returning_all_matches and Tournament::play_series with the veto output list.

## [3] Tournament prize money  {prio=high effort=small}
WHERE: GameManager.cpp:948-993 and 555-564 (finish paths), cpp:7349-7352 (phantom projection)
Winning Champions currently pays $0 — champions get a trophy and news only (verified: play_tournament_round's finished block at GameManager.cpp:948-993 touches no budget, and grep for 'prize' finds nothing but the projection). Worse, project_team_finances already books a 'prize expectation' of $120K/$40K/$15K by tier (cpp:7350-7352) as income that can never materialize, so every club's wage envelope is inflated by phantom money. Pay tiered purses (e.g. Champions $400K/$200K/$100K, Masters and Regionals scaled down) at both finish paths — the natural finish block and force_finish_stale_tournaments (cpp:555-564) — plus a mail to the user. Tiny engine work, and it makes deep playoff runs financially strategic (fund the re-sign pass with a Masters run).

## [4] Manager job security: board trust, sackings, and a job market  {prio=high effort=medium}
WHERE: GameManager.cpp:3308-3390 (judge_board_objective), cpp:3113-3121 (ambition, unread)
Board objectives are issued, tracked with live progress bars, and judged with a full scorecard — but the verdict is mail-only ('Serious questions hang over the offseason', GameManager.cpp:3364-3365, verified no consequence follows) and board_ambition_ is computed but never read (cpp:3121). Add a persistent board-trust meter fed by the met_n/objectives ratio in judge_board_objective, placement vs board_target_placement_, and difficulty; two bad seasons below threshold triggers dismissal. On sacking, offer the FM experience: take over another club (all club data, budgets, and rosters already exist — reuse the New Game wizard's team-selection plumbing at cpp:360-514) or game over with a career summary. This converts the objectives system from flavor into the game's core tension.

## [5] Training focus / individual development plans  {prio=high effort=medium}
WHERE: Player.cpp:1674-1699 (dead PerfBuckets), Player.cpp:1884-1900 (growth scaling), GameManager.cpp:1003-1048 (run_monthly_progression)
Monthly progression is pure age-curve + noise — the performance-targeted growth described in the big comment at Player.cpp:1646-1672 was removed, and its engine, compute_perf_buckets (Player.cpp:1674-1699), survives as fully-implemented dead code. Let the user assign each player a training focus (Mechanical / Game-IQ / Athletic per the existing attr_aging_class mapping at Player.cpp:1705-1740, or a signal cluster from PerfBuckets) that tilts which attributes the monthly pass hits, scaled by the coach's dev_focus lean (Team.cpp:962-988). Follow the map_prep pattern (Team.h:397-402): a user-only focus map that is empty for all AI teams, so the dynasty sim path is a provable no-op and DYNASTY-DETERMINISM holds. This is the classic FM 'develop your wonderkid your way' loop the game's deep potential/scouting systems are begging for.

## [6] Loan system to Tier 2/3 clubs  {prio=high effort=large}
WHERE: GameManager.cpp:6391-6470 (lower-tier machinery), Team.cpp:500-582 (sign_player role-lock semantics to mirror), Player.cpp:1466-1507 (discontent it fixes)
Lower tiers play real simulated rounds (play_tier_stage_round, GameManager.cpp:6450-6470) but there is no loan mechanic (verified: zero 'loan' hits in cpp/src). Benched young players currently rot: mood_breakdown hands out -12 for <4 season matches (Player.cpp:593) and <8 matches accrues +0.15 discontent at year-end (Player.cpp:1466-1507). A season loan — player keeps his contract (add loaned_from/loaned_to team ids), joins a T2/T3 roster for the year, accrues season_matches and coach-driven monthly growth there, returns in run_end_of_year before roster cleanup (cpp:2633) — solves bench discontent, gives the fog-of-war scouting system a reason to watch T2 (your own loanee's form), and deepens the tier pyramid that already exists.

## [7] Contract clauses: release/buyout, performance bonuses, team options  {prio=high effort=medium}
WHERE: Player.h:251-261 (Contract), Player.cpp:3150-3508 (evaluate_resign_offer), Team.cpp:3065 (will_sell)
The Contract struct holds only salary, expiry, signing bonus, and starter/role promises (Player.h:251-261, verified). Add negotiable clauses: a buyout/release clause that overrides Team::will_sell (Team.cpp:3065-3069) when a bid meets it — players demand one to join smaller clubs, clubs pay a wage premium to omit it; per-title and MVP performance bonuses settled in the year-end finance pass (GameManager.cpp:1747-1913); and a team-option final year. Each clause is one more scored term in evaluate_resign_offer's transparent factor list (Player.cpp:3150-3508) — the negotiation modal already renders per-factor deltas and counter-offers (app.html:6350-6607), so the UI cost is a few extra sliders. This is the highest-leverage way to make negotiations feel like FM rather than a price check.

## [8] Real transfer windows with a deadline day  {prio=medium effort=medium}
WHERE: GameManager.h:567-574 (annotated stub), GameManager.cpp:3850-3921 (user bid path), cpp:1320-1508 (mid-season replacements)
transfers_open() is a hardcoded 'return true' whose own comment is a TODO list of the AI paths a windows feature must wrap (GameManager.h:567-574, verified). Implement two windows — offseason plus a mid-season window around STAGE 2 — gate the user's Make-Offer/FA-sign paths and the listed AI paths (B8 walk-poach, ai_full_offseason_pass, run_mid_season_replacements, each before its release_player per the comment's never-free warning), and add a deadline-day beat: a burst of AI activity, rumor news, and last-hour incoming offers on the final window day. The calendar screen (GameManager.cpp:1671-1704) shows window open/close dates. Scarcity is what makes transfer decisions tense; right now signing is possible 200 days a year so no move ever feels urgent.

## [9] Retired pros enter the staff market (pro-to-coach pipeline)  {prio=medium effort=small}
WHERE: GameManager.cpp:1984-2005 (retirement rolls), cpp:6865-6938 (coach market), Coach.cpp:154-182 (generation to adapt)
Staff markets restock exclusively via generate_coach/scout/analyst with random names (verified GameManager.cpp:6928-6933) — retired legends simply vanish into the Hall of Fame. At the retirement roll (cpp:1984-2005), have high-Leadership retirees and career IGLs (is_igl, igl_impact_total) with 5+ seasons convert into a Coach or Analyst seeded into free_coaches_/free_analysts_, with reputation derived from accolade_score() and archetype from their playstyle. Add news: 'Former World Champion X joins the coaching ranks.' The payoff-per-line-of-code is enormous for a dynasty game: ten seasons in, you hire the IGL who beat you in the Year-2 Grand Final, and HoF careers stay alive in the world instead of ending at induction.

## [10] Locker-room events with manager decisions  {prio=medium effort=medium}
WHERE: GameManager.cpp:3050-3072 (mail_user), vlrweb.cpp:787-1024 (act dispatch), Team.cpp:2829 (chemistry write)
All the state for FM-style squad drama already exists — mood, discontent, ego (mood_breakdown docks -3 per ego>=75 teammate, Player.cpp:536-619), pairwise chemistry, promises — but mail is strictly read-only (act verbs are mailread/mailstar/maildelete only, vlrweb.cpp:989-1007). Add a decision-mail type: occasional events (two high-ego stars feud, a rookie asks for a mentor, a player skips scrims) presenting 2-3 choices; a new act:decision:<event_id>:<choice> verb resolves them by writing existing fields — discontent, mood, chemistry via Team::record_chemistry (Team.cpp:2829). MailItem needs an optional decision payload (it already stores player_id, GameManager.h:437-475) and the events must fire only for the user club via the mail_user chokepoint so the sim path stays deterministic. This turns the personality systems from passive dials into gameplay.

## [11] Scrims on off-days (chemistry + map reps)  {prio=medium effort=small}
WHERE: Match.cpp:2326-2331 (friendly gate), gui_match.inc:26-44 (existing friendly driver), Team.cpp:2829-2841 (record_chemistry)
The friendly-match path is already fully built and career-safe — friendly maps compute viewer stats but skip all career/season writes (verified Match.cpp:2326-2331) — and it currently powers only the bracket 'Watch' button. Add an act:scrim:<team_id>:<map> verb usable on off-days: play one friendly map against a chosen sparring partner, watchable in the live viewer, granting a small pairwise chemistry bump via record_chemistry (clamped like match play, Match.cpp:2492-2572) and a light mastery rep on the scrimmed map. Cost it (travel $, or a weekly scrim cap) so it's a scheduling decision, not a free stat pump. User-initiated only, so the dynasty path never changes. This gives the empty off-days between the every-3rd-day stage cadence (GameManager.cpp:31-57) something to do.

## [12] Persistent rivalries with derby stakes  {prio=medium effort=medium}
WHERE: GameManager.cpp:6048-6050 (rivalry emitter), cpp:984-992 (h2h finals log), Match.cpp:21-43 (tilt clamp)
Rivalry ingredients exist but never cohere: emit_rivalry_match_night fires news off h2h>=4 counters (with its tournament path admittedly 'stubbed', GameManager.cpp:6048-6050), and h2h_finals_log_ records every Grand Final pairing (cpp:984-992) then gets cleared yearly. Promote to a persistent Rivalry record keyed by team-id pair, with intensity fed by finals meetings, playoff eliminations, and poached players (readable from both clubs' transfer_log_, Team.h:181-188). Derby effects: a small bounded tilt through the existing wsb_match_tilt clamp ([0.95,1.05], Match.cpp:21-43), a win bonus to fan buzz/sponsorship, pre-derby board mail, and a rivalry panel on the team profile (currently data-starved per DashboardExport.cpp:2008-2017). Rivalries are the cheapest narrative flywheel a dynasty sim can own.

## [13] Fan support economy driven by community rankings  {prio=medium effort=medium}
WHERE: GameManager.cpp:5518-5560 (community score), cpp:834 (stipend), cpp:6843-6863 (sponsorship EMA)
Community rankings already model hype precisely — 0.50 lagged results + 0.25 star power + 0.15 flashy highlights + 0.10 news buzz (GameManager.cpp:5546) — but the score feeds nothing. Derive a per-club fan_support value from community rank and star signings; scale the currently-flat $10K matchday stipend (cpp:834) and the dynamic sponsorship EMA (cpp:6843-6863) by it, and surface it as a Finance dashboard KPI. This creates FM's classic commercial tension: a flashy Duelist superstar is worth wins AND revenue, and a boring grinder team wins matches while the money side stagnates. Small engine surface since both income sites and the ranking computation already exist.

## [14] Youth academy with non-roster development slots  {prio=medium effort=large}
WHERE: SoloQ.cpp:263-266 (ladder), GameManager.cpp:1003-1048 (dev multiplier seam), cpp:3871 (roster cap check)
The SoloQ ladder is the de facto youth pool (900 prospects per region simmed daily, SoloQ.cpp:263-266) but the only way to develop a prospect is spending one of 7 hard roster slots (cap enforced at GameManager.cpp:3871). Add 2-3 academy slots: cheap development contracts for 16-19yo prospects who stay on the ranked ladder (already simulated every day) but train under your coach — swap their FA 0.95x dev baseline for the team coach's dev_growth_factor in run_monthly_progression (cpp:1003-1048) — with a year-end promote/extend/release decision. Scouting fog reveals and the recruitment hub already surface exactly these players. This is the 'AcademyPowerhouse' brand tag (Team.cpp:238) made real instead of cosmetic.

## [15] Pre-contract agreements for expiring players (Bosman signings)  {prio=medium effort=medium}
WHERE: GameManager.cpp:2185-2473 (re-sign pass), DashboardExport.cpp:2486-2581 (negotiation preview reuse)
Expiring contracts (years_left<=1) already drive the AI's B7 re-sign pass and B8 walked-star bidding war (GameManager.cpp:2185-2622), but the user cannot act on an expiring rival player until he actually walks into the offseason FA scrum. Add a pre-contract window from REGIONALS 3 onward: approach an expiring player at another club, negotiate through the existing evaluate_resign_offer preview channel (already serves FAs, DashboardExport.cpp:2496-2507), and agree a no-fee deal that executes at year rollover — with the seller club's players/board reacting (mood, rivalry fuel). The B7 pass must check for an agreed pre-contract before counter-offering. This adds a whole planning layer to the season's back half at moderate cost.

## [16] User-appointed IGL with real tradeoffs  {prio=low effort=small}
WHERE: Team.cpp:654-774 (election + stat shift), vlrweb.cpp:787-1024 (act dispatch)
The in-game leader is auto-elected by enforce_one_igl after every roster mutation (Team.cpp:654-774) and the user has zero say, despite IGL being a headline mechanic (IGL badges, IGL-of-the-Year race, igl_impact scoring). Add act:setigl:<player_id> for the user team only: restrict to candidates passing the existing mental gate (Duelist veto unless mental>=0.80, Team.cpp:676), apply the canonical symmetric stat shift (Team.cpp:637-650), and have the deposed IGL take a mood/ego hit through the existing grievance fields. Small, self-contained, and gives the player agency over the identity decision the sim already treats as the most important on the roster.

## [17] Awards ceremony night on the AWARDS calendar day  {prio=low effort=medium}
WHERE: GameManager.cpp:4774-5215 (awards data), cpp:1092-1109 (AWARDS day), vlrweb.cpp:144-186 (screen registration)
AWARDS is a real 1-day phase (kPhases, GameManager.cpp:15-21) but it plays as an instant run_end_of_year tick; the rich award data (MVP with per-factor breakdowns, role awards, ROTY, Most Improved, Coach of the Year — cpp:4774-5215) arrives as a static recap screen. Stage it: a 'ceremony' screen revealing winners category-by-category with nominee shortlists (the mvp_race top-3 already exists as the shortlist), your players highlighted in club color, and reaction mail (a snubbed star gains discontent — one write to an existing field). Engine cost is near zero since last_season_awards already carries everything; the work is one new screen name in the export_screen/safe_screen pair (vlrweb.cpp:144-186) plus UI. Turns the season's emotional payoff into an event.

## [18] Dynasty yearbook: per-season club archive  {prio=low effort=small}
WHERE: GameManager.cpp:1706-2973 (run_end_of_year snapshot point), DashboardExport.cpp:2008-2017 (thin history rows), SaveGame.cpp (serialization)
Team history persisted per season is just year/wins/losses (DashboardExport.cpp:2008-2017, noted data-starved), so a 10-year dynasty has no browsable story. At the end of run_end_of_year, snapshot a SeasonRecord per club — final placement, playoff finish, trophies won, top-rated player id, biggest signing/sale from transfer_log_, board verdict for the user — into a small archive on Team, serialized in SaveGame. Surface as a 'History' tab on the team profile and a year-end yearbook mail. This is cheap (all values are computed during run_end_of_year already, they're just discarded) and it's what makes long saves feel like an era rather than a scoreboard.

# CATEGORY: qol

## [1] No unread-mail indicator anywhere outside the Mail screen  {prio=high effort=small}
WHERE: ui-poc/app.html:2406, cpp/src/DashboardExport.cpp:1284
The engine exports an unread count (DashboardExport.cpp:1284 emits it, but only inside the mail payload) and mail carries actionable items (transfer requests, Grand Final notices, board verdicts), yet the nav rail's Mail button (app.html:2406) has no badge and the dashboard has no mail KPI or nudge. A player who doesn't ritually open Mail after every Continue can advance past a transfer-request window without ever knowing it arrived. Fix: add unread_mail to export_dashboard (it's one kv_int) and paint a count dot on #navMail in every render — the nav is static markup so one shared helper can update it from any state push that includes the count.

## [2] Continue/Phase silently sim your own match — even your playoff run — with no guard  {prio=high effort=medium}
WHERE: ui-poc/app.html:2413-2415, 8023-8031; cpp/src/vlrweb.cpp:121-141
advance('day') on a matchday plays the user's series (GameManager sims every matchup; vlrweb.cpp:112-120 quietly captures the recording), and advance('phase') loops up to 80 days (vlrweb.cpp:131-136), which during REGIONALS/CHAMPIONS sims the user's entire bracket run off-screen. Neither button confirms, and both are one pixel below the always-visible Continue in the cmdbar (app.html:2413-2415). One muscle-memory click on Champions matchday skips the game's flagship live viewer for the biggest series of the season. Fix: when the dashboard export's next_match.days_until===0 (already available client-side), have advance() interpose a one-click confirm ('Your match is today — Sim it, or Enter Matchday?'); same for Phase while the user is in an active bracket (export_brackets already carries a user-participation flag).

## [3] Favorites and Watchlist screens are unreachable — watch statuses and notes are dead features  {prio=high effort=small}
WHERE: ui-poc/app.html:2392-2409 vs 3693-3766, 7983
renderFavorites/renderWatchlist are fully built (app.html:3608-3766) and setNavActive even maps them to #navFav/#navWatch (app.html:7983,7989) — but those buttons don't exist in the nav markup (app.html:2392-2409) and no go('favorites')/go('watchlist') call exists anywhere; the only entry is a hand-typed URL hash at boot (app.html:8318). The watch-status select (Watching/Scouting/Bidding) and the per-player note input exist ONLY on the watchlist screen, so those persisted engine features are unreachable in normal play; Recruitment's Shortlist tab shows star/eye toggles but no status or notes. Fix: add the two rail buttons (icons already follow a pattern), or fold status/note editing into the Recruitment Shortlist tab rows.

## [4] Zero keyboard shortcuts for the core loop — Continue is a mouse-only button pressed hundreds of times  {prio=high effort=small}
WHERE: ui-poc/app.html:6949, 6967, 8023-8035
The only document-level key handlers in 8,333 lines are Escape-to-close on the two modals (app.html:6949, 6967); everything else is per-row Enter/Space activation. Advancing a 200-day season means clicking the 34px-wide Continue button in the bottom-left corner over and over; there is also no key for 'skip to match', no digit keys for the 14 nav screens, and no '/' to focus the Recruitment search. Fix: one document keydown listener that ignores events from input/select/textarea and maps Space/Enter→advance('day'), M→advance('match'), plus 1-9 for nav go() calls — advance() and go() are already globally exposed (app.html:8034-8035).

## [5] Transfer-bid feedback: the seller's real asking price lives only in a 2.6-second toast  {prio=high effort=medium}
WHERE: ui-poc/app.html:6427-6434, 7730; cpp/src/GameManager.cpp:3864-3889
The bid pane's pre-bid verdict compares your fee to transfer_value_k (app.html:6427-6434, hint line at 7730), but sellers actually accept at sell_threshold_k — market value times a strategy multiplier up to 1.55x plus a +0.60 core-piece premium (Team.cpp:3025-3069) — so 'Around market value — could go either way' is systematically wrong for contender-owned stars. The engine computes the true ask on every bid (o.asking_k, GameManager.cpp:3864) and reveals it in the rejection message (GameManager.cpp:3887-3888), but that arrives only as a single toast that auto-fades in 2.6s and is replaced by any other toast (app.html:6258-6273); rejected bids send no mail (only accepted ones do, GameManager.cpp:3908). The player must screenshot or memorize the number. Fix: persist the last rejection (asking_k) into export_player_interest or the bid pane payload so re-opening the Bid tab shows 'Last bid rejected — they want ~$NK', and/or mail rejections like acceptances.

## [6] Market's one-click Sign spends the full ask instantly — no confirm, and a refusal permanently sours the player  {prio=high effort=small}
WHERE: ui-poc/app.html:4927, 4992-4994; cpp/src/vlrweb.cpp:577-604
The Sign button on every Market FA row posts act:signfa immediately (app.html:4927, 4992-4994); the host's quick path meets the player's full ask (vlrweb.cpp:595-599) with no confirmation step, while the player modal's Sign tab offers a full negotiation form that routinely signs the same player for less. Worse, if the quick offer is refused the host calls register_rejected_offer (vlrweb.cpp:603), which raises the player's mood-inflated ask by design (Player.cpp mood pipeline) — so an accidental click has a lasting cost either way. This is inconsistent with the two-step armed confirms used for Release (app.html:7784-7797) and staff Fire (app.html:3272-3287). Fix: make the row button open the player modal's Sign tab (openPlayerModal + pmTab='sign' already exist), or at minimum use the same armed-confirm pattern showing the ask.

## [7] Mail: 40-of-150 hard cap, no search/filter/paging, and one-click Delete beside two-step Fire  {prio=medium effort=medium}
WHERE: ui-poc/app.html:4459-4528; cpp/src/DashboardExport.cpp:1289-1291
export_mail truncates to the latest 40 items while the mailbox holds up to 150 (DashboardExport.cpp:1291; GameManager cap at push_mail) — messages 41-150 are permanently invisible in the UI even though the header proudly reports the real total ('N messages', app.html:4457). There is no search box, no category filter (categories are rendered as colored pills but aren't clickable filters, app.html:4477), no starred-only view, and the only bulk action is Mark all read (4459-4463). Meanwhile Delete fires instantly with no confirm (app.html:4528) while firing staff requires an armed two-step — the inconsistency trains the wrong reflex. Fixes: clickable category pills that filter the client-side items array (pure JS), raise the export cap or page it, and give Delete the armed-confirm treatment.

## [8] Autosave silently does nothing for entire tournament phases  {prio=medium effort=small}
WHERE: ui-poc/app.html:8026-8029, 8280-8281; cpp/src/vlrweb.cpp:941; cpp/src/SaveGame.cpp:811-813
Settings promises 'Save to the AUTO slot after every advance' (app.html:8280-8281) and the shell posts act:save:auto:quiet on every advance (app.html:8026-8029), but SaveGame refuses while any tournament is active (SaveGame.cpp:811-813) and the host's quiet branch swallows the failure with no toast (vlrweb.cpp:941). Masters and Champions run 14-16 consecutive days, so a crash mid-Champions loses up to two weeks of a run the player believed was autosaved; the gate message only ever appears on manual saves. Fix: on a quiet-save refusal, post a one-time-per-phase warn toast ('Autosave paused during the bracket — will resume at phase end'), and have the host fire one automatic save the moment force_finish_stale_tournaments clears active_tournaments.

## [9] Player-modal Interest and Scout tabs serve stale, never-invalidated caches  {prio=medium effort=small}
WHERE: ui-poc/app.html:6318-6330, 7111
pmInterestCache/pmScoutCache persist for the whole session and pmInterestAsked/pmScoutAsked reset only when a DIFFERENT player id renders (app.html:6318-6330, 7111) — so closing a player's modal, advancing three weeks, and re-opening the same player repaints the old 'pursuing clubs' scan and old scout read with no refetch and no refresh affordance. Transfer interest is exactly the data that changes as budgets, role needs and form move day to day, so the player is making sell/hold decisions on expired intel without knowing it. Fix: stamp each cache entry with day_in_year/year from the payload and refetch when the world day has advanced, or add a small 'Rescan' button in the pane header that clears the asked flag and re-posts get:playerinterest/get:scoutreport.

## [10] Team talk is only settable on the dashboard, only on matchday — every other path to a match skips it  {prio=medium effort=small}
WHERE: ui-poc/app.html:2774-2787; cpp/src/GameManager.cpp:731-755
The one-shot team-talk edge (a real 1.5-2.5% series multiplier, GameManager.cpp:731-755) is only rendered as chips inside the dashboard's Next Match hero, and only when days_until===0 (app.html:2774-2787 — the sole act:teamtalk post site). Pressing the cmdbar's Match button from Roster, Strategy, or anywhere else sims the match with team talk unset, and the Strategy screen — the designated match-prep hub with map prep and comp overrides — has no team-talk control at all. Fix: add the same three chips to renderStrategy next to the opponent intel card (the header already carries team_talk state), and/or include them in the confirm dialog proposed for matchday advances.

## [11] Dashboard lies: hardcoded 'Stage 1 · Rd 9/12' footer and fake day-rail phase positions  {prio=medium effort=small}
WHERE: ui-poc/app.html:2536, 2684-2690
The standings footer span 'Stage 1 · Rd 9/12' (app.html:2536) is static skeleton text with no id — it displays that exact string in Year 5 during Champions, and the real stage is 11 rounds, not 12. The day-rail ticks are hardcoded percentages [Stage 1 @14%, Regionals @52%, Masters @74%, Champions @92%] over a magic 200 (app.html:2684-2690) that don't match the real 11-phase calendar, so the 'today' dot regularly sits in the wrong phase segment. Players use both to answer 'how long until playoffs?' and get wrong answers. Fix: the header already receives current_phase and the calendar export carries phase_idx/day_in_phase/phase_matchdays — set the footer text from those, and derive tick positions from the same phase pacing data renderCalendar already consumes.

## [12] Market FA table: no sort, no search, no role filter, and a hidden 40-player cutoff  {prio=medium effort=small}
WHERE: ui-poc/app.html:4963-4999; cpp/src/DashboardExport.cpp:1545
The Free Agents card renders a fixed OVR-desc list with 8 columns and zero controls (app.html:4963-4999) while the export truncates to 40 (DashboardExport.cpp:1545) and the counter shows the true total ('N available', app.html:4965) — early-season pools are far larger, so cheap role-specific targets below the OVR-40 cutoff simply don't exist on this screen. The Recruitment hub already implements clickable sort headers, role chips and search over the full pool (app.html:5524-5535, 5474-5495), so either reuse rcRenderHead's pattern on the market table, or replace the market list with a 'Browse all N free agents →' link that opens Recruitment pre-set to the Free Agents tab (rcState.tab='fa' is one assignment).

## [13] No day digest after advancing — Continue gives literally zero feedback  {prio=medium effort=medium}
WHERE: cpp/src/vlrweb.cpp:106-142, 780-782
The host's advance branch just re-posts the current screen's state with no toast and no summary (vlrweb.cpp:780-782), and GameManager's rich day log (round headers, series scorelines, champion announcements, 'Your match: vs X → 2-1') is collected into a log vector that the web host throws away (vlrweb.cpp:108). Pressing Continue from Mail or Roster, the only evidence anything happened is numbers quietly changing. A phase-skip that force-finished a bracket, paid a sponsor, or fired your board verdict is completely mute. Fix: return DayResult highlights (matches played, phase transitions, mail count delta) from do_advance and post one toast — or better, a small dismissible 'while you were advancing' digest — after multi-day advances (match/phase modes especially).

## [14] cmdbar Match strands you on the current screen after your match plays  {prio=medium effort=small}
WHERE: ui-poc/app.html:2414, 2790, 8023-8031
The dashboard's ENTER MATCHDAY CTA posts advance:match:match so you land on the live viewer (app.html:2790), but the identical cmdbar Match button posts advance:match:<currentScreen> (app.html:8025 via advance('match') at 2414) — press it from Roster and your series sims, the roster re-renders, and you must manually click the Match nav icon to watch the replay you just generated. Same action, two different outcomes, and the worse one is on the always-visible button. Fix: special-case mode==='match' in advance() to post 'advance:match:match' (one-line change), since the whole point of that button is seeing the match.

## [15] Watchlist rows omit every number a transfer shortlist exists to track  {prio=medium effort=small}
WHERE: ui-poc/app.html:3711-3728 vs 5501-5515
The watchlist table shows CTRY/PLAYER/ROLE/AGE/OVR/TEAM/STATUS/NOTE (app.html:3711-3728) — no market value, no wage, no contract years left, no potential band, no wants-out flag — so evaluating your own shortlist means opening each player's modal one by one and remembering the numbers across modals. The Recruitment export already ships value_k, wage_k, years_left, pot bands and wants_out per row (app.html:5501-5515 renders them), so either add those columns to export_watchlist/renderWatchlist, or make the Recruitment Shortlist tab (which has all the columns) the canonical watchlist and give it the status/note controls.

## [16] Escape closes both stacked modals at once  {prio=low effort=small}
WHERE: ui-poc/app.html:6949, 6967
Opening a player modal from inside a staff modal (or vice versa) leaves both open flags true, and each has its own document-level Escape listener that checks only its own flag (app.html:6949, 6967) — one Escape press slams both closed, dumping the player back to the base screen instead of the modal they came from. Fix: have the player-modal listener stopPropagation/return when it handles the key, or close only the most recently opened modal by tracking a simple open-order stack.

## [17] Recruitment infinite scroll re-filters and re-sorts the entire world pool on every scroll event  {prio=low effort=small}
WHERE: ui-poc/app.html:5727-5731, 5537-5545; cpp/src/DashboardExport.cpp:3898-3947
The scroll handler calls rcFiltered() — a full filter pass plus sort over the uncapped world player export (thousands of rows, DashboardExport.cpp:3898 ships everything) — on every scroll tick just to read .length (app.html:5727-5731), and rcRenderBody(false) then runs the same filter again. On the game's biggest table this makes the flagship FM-style screen feel janky exactly when the player is browsing hardest. Fix: cache the filtered array on rcState when filters/sort change (rcRenderBody already knows when reset=true) and have the scroll handler read the cached length.

## [18] God-mode editing is one-unit-per-round-trip stepper clicking with no direct entry  {prio=low effort=small}
WHERE: ui-poc/app.html:3200-3223, 7946-7961
Attribute and potential editors are ±1 steppers that capture the render-time base and no-op further clicks until the host re-pushes and fully re-renders the panel (app.html:3200-3223, 7946-7961) — deliberately safe against double-posts, but raising Aim from 62 to 90 is 28 click-wait-rerender cycles, and a player has ~20 attributes. The bridge already accepts arbitrary clamped values (act:setattr:<id>:<attr>:<val>, vlrweb.cpp clamps 1-99), so the UI could offer a click-to-type number input on the .av value span (commit on Enter/blur, same pattern as the staff-modal rename at app.html:7073) with the steppers kept for nudges.

# CATEGORY: visual-polish

## [1] Standings footer permanently displays hardcoded 'Stage 1 · Rd 9/12'  {prio=high effort=small}
WHERE: ui-poc/app.html:2536
DASH_SKEL bakes the literal text 'Stage 1 · Rd 9/12' into the dashboard standings footer (app.html:2536) with no id, and renderDashboard never touches it — a grep confirms this string exists only in the skeleton. The player sees 'Stage 1 · Rd 9/12' in Year 4 Champions week; it reads as live data (styled like the two real legend entries next to it) but is a lie 100% of the time after the first round. Give it an id and populate it from D.header.current_phase + the round counter the engine already exports, or delete the span.

## [2] Day-rail phase ticks are hardcoded percentages that don't match the real 11-phase calendar  {prio=high effort=medium}
WHERE: ui-poc/app.html:2684 (and 2662/2664/2685)
renderDashboard positions the four rail labels from a hardcoded table PHASES=[["Stage 1",0.14],["Regionals",0.52],["Masters",0.74],["Champions",0.92]] (app.html:2684) over a magic 200-day year repeated at :2662, :2664 and :2685. The engine actually runs 3 stages, 3 regionals and 2 Masters (GameManager kPhases), so the rail shows 'Regionals' lit at day ~104 while the player is in STAGE 2, and any calendar rebalance in C++ silently invalidates all four positions. The calendar exporter already ships the full phase ring with day spans (export_calendar) — derive tick positions from that payload instead.

## [3] 8-8.5px --fg-ghost labels on bg-2/bg-3 surfaces fail WCAG AA contrast  {prio=high effort=small}
WHERE: ui-poc/app.html:385, 406, 1205, 1903 (token at :23)
--fg-ghost #7C8499 computes to ~4.35:1 on --bg-2 #1C202C and ~3.87:1 on --bg-3 #242938 — both below the 4.5:1 AA minimum, at the smallest text sizes in the app. Affected: .stat .l 8px on bg-2 (app.html:385-387), .cbox .l 8px (:406-408), .pmStat .l 8px (:1205-1207), .pmCbox .l (:1235-1237), .ovrbox .l 8.5px (:380-382), .rsAo .rsAolab 8px on bg-3 (:1903-1904), .skEst .el on bg-3 (:2081-2083). These are the stat-tile captions the player reads constantly (OVR/POT/K-D labels). Bump these labels to --fg-muted (#8B93A3, ~4.64:1 on bg-1 and better on bg-2) or lighten --fg-ghost.

## [4] Font weights 700 (Inter, IBM Plex Mono) and 800 (all families) are used but never loaded — browser-synthesized faux bold  {prio=high effort=small}
WHERE: ui-poc/app.html:12 (uses at 124, 1366, 1537, 1633, 1688, 2161, 2198)
The Google Fonts link (app.html:12) loads Space Grotesk 500/600/700, Inter 400/500/600, IBM Plex Mono 500/600. Yet font-weight:700 is applied to Inter-default UI text everywhere (.klab :204, .pill :257, every sticky table header) and to mono numerals (.recrank :1537, .mvNums :1633, .mvBoardHead .sc :1715, .skCovbig .v :2070, .recscore :1557), and weight 800 appears at .mmLogo :124, .piBadge :1366 (mono 800!), .mvMvpName :1688, .ngCrest/.ngTitle :2161-2163, .ngStart :2198. Chromium synthesizes these by smearing outlines, so the biggest hero numbers (match score 52-60px, main-menu logo 64px) are the blurriest text in the game. Add 700 to Inter and Plex Mono and 700→use instead of 800, or add the missing axes to the CDN request. Related: the CDN link means an offline WebView2 first run silently falls back to system-ui/monospace, changing the entire look — consider shipping the woff2 files locally.

## [5] Favorites, Watchlist, Records and Match screens show a stale hardcoded 'O' crest instead of the club initial  {prio=medium effort=small}
WHERE: ui-poc/app.html:3596, 3678, 3782, 3993
The header skeletons at app.html:3596 (favorites), :3678 (watchlist), :3782 (records) and :3993 (match) hardcode <div class="hcrest">O</div>, and unlike every core screen (setters at :2661, :2944, :3336, :3453, :4445...) their renderers never assign $("#hcrest").textContent. A club named 'Vortex' shows a 'V' crest on Dashboard and an 'O' crest on these four screens — a visible identity glitch every visit. renderScouting (:5151) also sets #hcrest but skips #navCrest. Copy the two-line mono/crest snippet from renderDashboard (:2660-2661) into these renderers.

## [6] 'ENTER MATCHDAY' — the game's primary CTA — has no hover or active state  {prio=medium effort=small}
WHERE: ui-poc/app.html:301-304
.hero .cta is defined at app.html:301-304 with .live (red glow) and .soon variants, but a grep confirms no .cta:hover or :active rule exists anywhere. Every other primary button in the file has an explicit hover (.signbtn:hover :778, .smhire:hover :815, .rsSubmit:hover :1940) and .signbtn/.smhire/.mobtn even get :active translateY(1px). The most-pressed button in the whole loop gives zero pointer feedback, which reads as broken/disabled. Add .hero .cta.live:hover{filter:brightness(1.1)} (matching .mvSkip:hover at :1654) plus an :active nudge; also give .rsSubmit/.skCommitbtn/.ngStart the same :active treatment for consistency.

## [7] Click targets down to 14x14px — god-mode steppers, badge remove, row actions all below the 24px minimum  {prio=medium effort=small}
WHERE: ui-poc/app.html:660, 678, 1051, 1445, 2331
Interactive controls under WCAG 2.5.8's 24x24 floor: .stepbtn 16x16 (app.html:660-661, the +/- attribute steppers the user clicks repeatedly in god mode), its ovrbox variant 18x18 (:669), .gbx badge-remove x 14x14 (:678-679), .pmOpenBtn 20x20 (:1051-1052), .favunfav/.wlunwatch 22x22 (:1445-1446, :1496-1497), .rcAct star/eye 22x22 (:2331). The steppers are worst: they only move 1 point per host round-trip, so users hammer a 16px target. Raise all of these to >=24px (or add padding + negative margin to grow the hit area without changing visuals) — the row heights (42-46px) have room.

## [8] Gold vs amber confusion: Series-MVP card is amber under gold labels, and Records uses two different silver/bronze palettes  {prio=medium effort=small}
WHERE: ui-poc/app.html:1684-1699, 1540-1541, 1591
--gold is #FFD700 and --amber #D4A14C (app.html:26), but the Series-MVP crest hardcodes an amber gradient #ffd97a→#d4a14c (:1684-1685) while its 'Series MVP' label uses var(--gold,...) bright gold (:1687) — the marquee card is two-toned by accident. The idiom var(--gold,#d4a14c) at :1677, :1681, :1687, :1699 uses the AMBER hex as a fallback for gold, so if --gold is ever renamed these silently turn amber. Worse, the Records screen has two unrelated silver/bronze systems side by side: GOAT rank chips .recrank.g2/.g3 use #E6E9EF/#B9C0CC and #E4A66A/#C77B3C (:1540-1541) while the leaders boards .reclrow.t2/.t3 use #C8D0DC and #D69A5C (:1591). Promote --silver/--bronze tokens and pick one gold treatment for MVP surfaces.

## [9] Danger text color #ff8a94 hardcoded 34 times across CSS and JS instead of a token  {prio=medium effort=medium}
WHERE: ui-poc/app.html:258, 313, 1828, 3033, 3113 (34 occurrences)
The soft-red used for every negative/danger reading appears as a raw literal 34 times: CSS (.pill.primary :258, .seg.l :313, .rbadge.out :364, .fseg.l :476, .brktsub.lower :900, .pmDanger :1828, .fireBtn :1832, .skWeak :2115/2120, .rcTag.wants :2325...) and JS ternaries (bad ratings :3033/:6198, trajectory :3113, mood :3114/:7086, chemistry :3294, scout weaknesses :6840, mastery deltas :7422/:7458). --red exists but is too saturated for text on dark, which is why this shadow palette grew. Add --red-text:#ff8a94 (and optionally --red-border:rgba(255,70,85,.4)) to :root and replace-all — this is the single highest-leverage tokenization fix since the god-mode club color feature retints --club but can never retint these literals.

## [10] MVP-race analyst blurbs are crushed into a 74px box and render ~5 characters  {prio=medium effort=small}
WHERE: ui-poc/app.html:849-851, 5825
.racetrend is fixed at width:74px (app.html:849) and must hold both the ellipsized .rblurb (10px text, :850) and the .racedelta arrow (~30px, :851). renderAwards pours full engine sentences into it ('Riding the rookie hype train.') at :5817-5825, so the player sees 'Ridi…' — authored flavor text that is effectively invisible. Either widen the .racerow grid (:835 is 26px 1fr auto auto — the blurb could be a second row spanning all columns like .pmAgentrow's grid-template-areas does at :1300) or move the blurb to a title tooltip and drop the visible fragment.

## [11] Team-profile KPI chips claim to reuse the shared .kpi look but drift on value size and tile height  {prio=medium effort=small}
WHERE: ui-poc/app.html:968-975 vs 202-211
The comment at app.html:966 says .tpidentity 'reuses the shared .kpis/.kpi look', but .tpchip .tpcval is 22px (:971) vs .kpi .kval 28px (:205), .tpchip has no height while .kpi is fixed 90px (:203), and renderDashboard adds the bevel-tr corner cut to KPIs (:2697) that team-profile chips never get. Same semantic component (KPI tile with label/value/subline/hover bar), visibly different scale one click apart — the standings row → team profile transition makes the mismatch obvious. Either share the .kpi class with a modifier or copy height:90px, 28px value and the bevel.

## [12] Card/section header font size drifts 11/12/13px for the same semantic level  {prio=medium effort=medium}
WHERE: ui-poc/app.html:193, 293, 832, 887, 1584
The canonical card title .chead .t is 12px/700 uppercase (app.html:193), but the identically-ranked headers elsewhere are 13px (.tourn .tname :887), 11px (.hero .hh .t :293, .racecard .rctitle :832, .recboardhead :1584, .smrolehead .smtitle :787). Micro-labels are a near-continuum too: 8, 8.5, 9, 9.5, 10, 10.5px all mean 'small uppercase label' with letter-spacing wandering .03em-.14em (.stat .l 8px :387, .ovrbox .l 8.5px :382, .pill 9px :257, .rhead 9.5px :351, .klab 10px :204, .plate 10.5px :178). Define 3 label tiers (e.g. 12px card-title, 9.5px column-header, 8.5px tile-caption) as tokens and sweep — the audit found no intentional rationale for the in-between values.

## [13] Table row heights span 30-50px across ~15 cloned table families with no rhythm; the flag chip alone has 7 diverging clones  {prio=medium effort=large}
WHERE: ui-poc/app.html:270-2378 (table families), 1431/1468/1542/1592/2004/2319/1146 (flag clones)
Row heights per screen: .histrow 30 (:869), .pmHistrow/.tphrow 32 (:1262/:1033), .racerow 34 (:835), .srow 36 (:270), .csrow/.wrow/.pl 38 (:459/:503/:317), .mvRow 40 (:1722), .rrow/.tprow 42 (:354/:992), .smrow 44 (:801), .farow/.favrow/.recrow/.rcRow 46 (:754/:1426/:1532/:2310), .wlrow/.rcStRow 48 (:1464/:2378), .mrow 50 (:539). The 'uniquely-named so they never collide' duplication strategy (13 comment blocks admit it) guarantees this drift: the country-flag chip exists as .favflag :1431, .wlflag :1468, .recflag :1542 (9.5px, 3px pad, 36px min) vs .reclflag :1592 (8.5px, 2px pad, 32px), .skFlag :2004 (9px, 2px 5px), .rcFlag :2319 (9px, no bg/border at all), .pmFlag :1146 (2px 6px). Pick 3 row heights (compact 32 / default 40 / roomy 48) and one .flag class; the same-content lists (favorites vs watchlist vs records) should not differ by 2px per row.

## [14] The 8pt spacing scale (--s1..--s7) is declared but used zero times — every gap/padding is a magic px  {prio=medium effort=large}
WHERE: ui-poc/app.html:34 (0 usages)
:root declares --s1:4px..--s7:32px 'spacing 8pt' (app.html:34), but grep finds 0 occurrences of var(--s anywhere in the 8333-line file. Real spacing is ad hoc: card gaps run 12/14/16px for sibling columns on the same screens (.grid gap:16 :245 vs .rcol gap:14 :347 inside .rgrid gap:16 :346), header margins 10/14/16px, and odd values like 7px/9px/11px/13px gaps appear throughout (.mmBtns gap:11 :127, .mmSlots gap:9 :137, .maprow padding:13px 6px :603). The declared system creates false confidence for future contributors. Either adopt the tokens in the highest-traffic components (cards, grids, KPIs) or delete the dead declarations and document the real convention.

## [15] Long club names wrap and clip inside the fixed 76px header — the only title without an ellipsis guard  {prio=low effort=small}
WHERE: ui-poc/app.html:176 (vs 375, 1082)
.header .hname (app.html:176) is 28px display text with no white-space:nowrap/overflow, inside .header which is height:76px overflow:hidden (:163-165). Every comparable title in the file guards itself (.pdname :375, .pmName :1082-1083, .oppcard .oppname :573-574, .mvTeamName .nm :1630 all have nowrap+ellipsis). The new-game wizard allows 28-char club names (maxlength="28", :5365) and team-profile headers show procedural rival names plus an inline .tppill (:961-965); at narrower windows or long names the second line renders half-clipped by the header's overflow:hidden. Add white-space:nowrap; overflow:hidden; text-overflow:ellipsis and min-width:0 on .htitle.

## [16] Scrollbar styling: three different widths, per-container duplication, and webkit rules that modern Chromium ignores  {prio=low effort=small}
WHERE: ui-poc/app.html:69, 196, 429, 1198, 2308
Custom scrollbars are re-declared per container with three widths: nav 5px (app.html:69), .cbody/.recbody 6px (:196/:1521), and 7px for #view.roster (:429), .brktscroll (:895), .pmBody (:1198), .piWrap (:1332), .mvwrap (:1606), #view.match (:1702), #view.scouting (:1974); the recruitment containers .rcScroll/.rcStScroll/#rcAssign (:2308/:2377/:2338) skip webkit rules entirely. Every one of these also sets scrollbar-width:thin, and since Chromium 121 the standard property disables ::-webkit-scrollbar styling — so in the WebView2 runtime all these custom widths are dead code and everything renders the native thin scrollbar anyway. Define one shared .scrollthin utility (or a body-level scrollbar-color/width) and delete the per-container copies.

## [17] Motion system drift: 20 'transition:all' rules and hardcoded .1s-.18s durations in newer sections  {prio=low effort=medium}
WHERE: ui-poc/app.html:448, 1880, 2087, 2236, 2310 (20 transition:all hits)
Older components enumerate transitioned properties with the tokens (transition:color var(--t-fast) var(--ease)... e.g. .nav button :83, .signbtn :777), but the newer half of the stylesheet uses transition:all in 20 rules (grep-verified: .rsCheckbox :1880, .rsRole :1891, .skCommitbtn :2087, .ngSeg :2175, .spChoose :2217, .mact :2236, .rcTab :2286, .rcStHire :2386...) and hardcoded durations that bypass --t-fast/--t-mid: .cswPill .12s (:448), .mvMapTab .15s (:1657), .ngCrest .18s (:2162), .rcRow .1s (:2310), .rcAct .1s (:2331). transition:all also animates layout properties on hover (a known jank source on 46px-row tables). Sweep to explicit property lists with the duration tokens.

## [18] Conflicting and dead CSS rules: duplicate .sfoot-right (gap 7 vs 9), undefined --font-text, empty .sch.user::before, exact-duplicate .ff .empty  {prio=low effort=small}
WHERE: ui-poc/app.html:423 vs 1831; 2054; 487; 341
Four verified hygiene defects: (1) .scard .sfoot .sfoot-right is defined twice — display:flex;gap:7px at app.html:423 and display:inline-flex;gap:9px at :1831; the later one silently wins so the staff-card footer gap is 9px while the rule a maintainer will find first says 7px. (2) .skDonerow .dn .dt sets font-family:var(--font-text) (:2054) which doesn't exist (:root defines --font-display/--font-ui/--font-num), so the completed-report date falls back to inherited display font — likely meant --font-ui. (3) .sch.user::before (:487) is an abandoned stub ({content:"";position:absolute} with no dimensions) — compare the working 3px bar at .srow.user::before :275; the user's schedule row is missing its edge marker. (4) .ff .empty (:341) duplicates the shared .empty (:214) declaration-for-declaration. All four are two-minute fixes.

## [19] Inline styles fighting the stylesheet: fully inline morale bar and per-callsite .empty repadding  {prio=low effort=small}
WHERE: ui-poc/app.html:3134; 2802, 3118, 3165, 5806
The roster detail panel builds the morale meter entirely inline — <span class="pt" style="width:84px;height:6px;background:var(--bg-3);border-radius:3px;overflow:hidden;display:inline-block"> grafted onto the .pbar .pt class (app.html:3134) whose stylesheet version is flex:1 — the class name is a lie and the widths can't be themed. Similarly the shared .empty class (padding:10px 2px, :214) is re-padded inline at nearly every call site: style="padding:2px 0" (:2802, :3118), style="padding:0" (:3165), style="padding:6px 2px" (:5806) — four different empty-state paddings on one screen family. Add a .moralebar class and .empty modifier classes (.empty.tight) instead.

## [20] Mobile media queries are unreachable-by-design and create contradictory hybrid layouts  {prio=low effort=medium}
WHERE: ui-poc/app.html:48 vs 1228, 1816, 1977, 2203, 2364
body forces min-width:1240px with html{overflow-x:auto} (app.html:47-48), yet the file carries max-width breakpoints at 560 (:2112), 620 (:2203), 640 (:1816, and a min-width:640 at :1847), 680 (:1228, :1240), 720 (:2232), 760 (:2279), 820 (:2364) and 1080px (:1977). Media queries respond to viewport width, so in a narrowed window (the WebView2 window is freely resizable from 1480x940) the queries fire — .pmAttrs collapses to one column, .skGrid2 stacks — while the surrounding layout is still laid out at 1240px behind a horizontal scrollbar: a state no one designed. Either drop the min-width floor and make the breakpoints real, or delete the queries; the current combination can only produce the hybrid.

# CATEGORY: flavor-text

## [1] Award mail subject doubles the phrase: 'named Duelist of the Year of the season!'  {prio=high effort=small}
WHERE: cpp/src/GameManager.cpp:4923
The single award-mail chokepoint builds every subject as `aw.winner->name + " named " + cat + " of the season!"` (GameManager.cpp:4923). With cat values 'Duelist/Initiator/Controller/Sentinel of the Year', 'IGL of the Year', 'Rookie of the Year', 'Flex of the Year' (GameManager.cpp:5004-5117), 7 of 9 award mails render grammar like 'Kaze named Duelist of the Year of the season!'. The player sees these every AWARDS day, on the most prestigious mails in the inbox. Fix in one place: drop the ' of the season' suffix when cat already contains 'of the Year' (or switch to 'named 2027 <cat>').

## [2] International events are literally named 'MASTERS 1' / 'CHAMPIONS' — screaming caps bleed into the game's biggest mails  {prio=high effort=medium}
WHERE: cpp/src/GameManager.cpp:680
Internationals are constructed straight from the ALL-CAPS phase string (GameManager.cpp:680-681), so the brackets screen header (DashboardExport.cpp:1853), match-viewer event line ('MASTERS 1 - Grand Final'), the GF heads-up mail 'Through to the Grand Final of CHAMPIONS!' (GameManager.cpp:932-934) and the title mail subject 'CHAMPIONS — Omega win CHAMPIONS!' (GameManager.cpp:4063, where the celebratory 'CHAMPIONS —' prefix collides with the event named CHAMPIONS) all shout at the player at their peak emotional moments. Regionals get nice names ('Americas Regionals 1', cpp:643-644) but internationals don't. Give internationals proper display names — even better, VCT-style host cities ('Masters Reykjavik', 'Champions Seoul') from a small rotating city table keyed off year; the substring matchers already tolerate suffixes the way the Regionals comment (cpp:635-639) documents.

## [3] Post-match storyline is 3 fixed sentences — the most-repeated flavor text in the game  {prio=high effort=medium}
WHERE: cpp/src/Match.cpp:2574-2586
Match::storyline has exactly three non-bomb variants: 'MASSIVE UPSET BY <team>!', 'Domination by <team> as expected.', and the fallback 'A closely fought tactical matchup.' (Match.cpp:2574-2586); the map chip in the viewer adds only 5 static labels (Flawless/Overtime thriller/Nailbiter/Domination/Solid win, DashboardExport.cpp:3693-3700). A player sims ~33 series a season and will read 'A closely fought tactical matchup.' dozens of times by year 2. All the ingredients for variety already sit in the record (map name, OT rounds, MVP name, clutches, retakes, comeback deltas). Reuse the deterministic pick(salt, variants) idiom from the scout notes (DashboardExport.cpp:3049-3056) keyed on match hash so replays stay stable: 'Comeback from 3-9 on Ascent', 'stax's 1v2 seals it in OT', etc.

## [4] Winning the world title reads exactly like winning a regional — the career-defining moments are undersold  {prio=high effort=medium}
WHERE: cpp/src/GameManager.cpp:4060-4067
The champion mail body is one template for every tier: 'Your club is crowned <tier_label> <year>. A grand-final win over X seals the title.' (GameManager.cpp:4060-4067). A Champions (world) title — the stated end-goal in the main-menu tagline 'Win Champions.' (app.html:8249) — gets the same two sentences as REGIONALS 1. Promotion to the top flight is likewise one clause: 'X finished top of Tier 2 and earns promotion to the top flight.' (GameManager.cpp:6637-6638). Add tier-differentiated celebration copy (board reaction, first-ever-title vs repeat, dynasty-tier callout via Team::dynasty_tier which already exists) and a matching relegation gut-punch; branch on gm state you already track (trophy_case count, board objective status).

## [5] Team-name verb agreement flips between singular and plural — sometimes for the same event on the same day  {prio=medium effort=small}
WHERE: cpp/src/GameManager.cpp:4042 vs 4063
The champion NEWS headline says '<team> wins <event>' (GameManager.cpp:4042) while the champion MAIL subject says '<team> win <event>!' (cpp:4063) — the player sees both within one advance. Elsewhere: 'Upset! X take down #2 Y' (cpp:6101), 'X poach Y from you' (cpp:2361), and the viewer chip '<Team> take it' (app.html:4165), versus 'X wins Coach of the Year' (cpp:5201). Esports coverage convention treats org names as plural ('Sentinels win'), which most of the copy follows — pick that as house style and sweep the singular 'wins' headlines (or vice versa), it's a handful of format strings.

## [6] Pronoun voice splits by subsystem: mail says 'they/their', scout reports and negotiation say 'he/his/him'  {prio=medium effort=medium}
WHERE: cpp/src/vlrweb.cpp:679; cpp/src/DashboardExport.cpp:3120-3239; ui-poc/app.html:6845
GameManager mail is consistently neutral ('p->name + " has announced their retirement"' cpp:2067, 'final year of their deal' cpp:3183), but the scout-note traits use he/him in nearly every variant (DashboardExport.cpp:3120-3239, e.g. 'shaky when the server goes quiet and it's all on him' :3147), the reputation-gate toast hardcodes 'a player of his stature' (vlrweb.cpp:679), Player desire/reject copy has 'Stays put when he can' (Player.cpp:2755) and 'Competitor in his prime' (Player.cpp:3325), and the UI hardcodes 'best of his kit' / 'for his level' with tooltip 'Relative to his own level' (app.html:6845). Either commit to he/him everywhere as scene voice or normalize the system surfaces (toast, tooltips, blurbs) to they/them — right now the same player is 'they' in mail and 'he' in the scout modal one click away.

## [7] MVP-race blurbs are four static words on a flagship screen  {prio=medium effort=medium}
WHERE: cpp/src/GameManager.cpp:4453-4457
Every candidate on every awards leaderboard carries blurb = 'Climbing' / 'Slipping' / 'Steady' / 'New entry' (GameManager.cpp:4453-4457, exported at DashboardExport.cpp:1715). Next to the richly-written scout notes this reads like debug output, and the per-factor mvp_score_breakdown already computed for the MVP board (cpp:4445-4446) is exactly the material for one-liners: 'A 1.31 split doing the talking', 'The IGL tax, paid back in calls', 'Clutch numbers carrying a mid rating'. Deterministic phrasing from the dominant breakdown factor keeps it dynasty-safe and gives the race actual narrative.

## [8] Single-template news bodies repeat verbatim: every rumor and every rivalry night reads identically  {prio=medium effort=medium}
WHERE: cpp/src/GameManager.cpp:6013, 6058-6060
The rumor body is always 'Sources close to the org suggest preliminary conversations are underway.' (GameManager.cpp:6013, up to 3 per pass), and the rivalry body is always 'The N-meeting head-to-head between X and Y adds another chapter on the schedule.' (cpp:6058-6060) — and because that emitter's dedup key embeds day_in_year (cpp:6048-6050) it fires on EVERY meeting once h2h>=4, so the feed fills with the same sentence. Port the pick(salt, variants) helper from DashboardExport.cpp:3049-3056 into these emitters (salt on team/player id) so each item gets one of 3-4 stable phrasings; for rivalries, fold in the actual h2h split ('X lead the series 7-4') which h2h_total's neighbors can supply.

## [9] Your own veteran's retirement can pass with zero notice — the send-off mail is legends-only  {prio=medium effort=small}
WHERE: cpp/src/GameManager.cpp:2027-2074
Both the retirement news tribute and the user mail are gated on `notable` = career_seasons_played>=3 AND hof_criteria_count>=4 (GameManager.cpp:2027, 2063). A six-season starter who never stacked HoF points simply disappears from your roster in the year-end cleanup (cpp:2674-2679) with no mail, no news, nothing to click. Add an ungated (but user-roster-only) modest farewell mail in the same block — 'X calls it a career after N seasons in your colors' — reusing the existing mail/retire dedup key; keep the big tribute for legends.

## [10] Hiring or firing a head coach produces two dry words and no story  {prio=medium effort=small}
WHERE: cpp/src/GameManager.cpp:6940-7035; cpp/src/vlrweb.cpp:863,909
user_hire_staff/user_fire_staff (GameManager.cpp:6940-7035) emit no mail and no news; the only feedback is the toasts 'Coach hired' / 'Coach released' (vlrweb.cpp:863, 909). In a manager sim, sacking your head coach mid-project is front-page material — real orgs post twitlongers about it. A one-mail-per-event with the staffer's archetype blurb ('Master of X's and O's era ends after 3 seasons') plus a Media news item would make the staff screen feel alive; all the data (personality_name, tenure via contract_exp_year, reputation) is already on the Coach/Scout/Analyst objects.

## [11] AI GM mode narrates its moves into a log the web frontend throws away  {prio=medium effort=medium}
WHERE: cpp/src/vlrweb.cpp:108; cpp/src/Team.cpp:2104-2377
All the well-written '[OFFSEASON GM] UPGRADE: X -> Y', 'Kept X — strong chemistry overrides poor form.' lines (Team.cpp:2104-2377) go into the advance-day log vector, but do_advance's local `log` is never posted to the web UI (vlrweb.cpp:108) — so a player who enables AI GM ('the AI now manages your roster', vlrweb.cpp:987) watches players appear and vanish with no explanation anywhere. Batch the user-team pass's lines into a single 'Front office report' mail via mail_user at the end of ai_full_offseason_pass's user invocation (GameManager.cpp:2765-2772) — the copy already exists, it just needs a delivery channel the web app renders.

## [12] The same three sponsors offer the same three deals every preseason, forever  {prio=medium effort=medium}
WHERE: cpp/src/GameManager.cpp:3645-3656
generate_sponsor_offers hardcodes exactly 'Apex Performance' (top-2), 'Velocity Energy' (win count), 'Ascend Apparel' (1.15+ player) (GameManager.cpp:3645-3656). By season 3 of a dynasty the preseason sponsor modal — a scripted season-start beat — is pixel-identical, and the payout mail always reads '<brand> is delighted…' (cpp:3712-3714). Add a pool of 8-10 esports-flavored brands (peripherals, energy, apparel, crypto-adjacent parody) selected deterministically by year+team id, and 1-2 body phrasings per requirement type; the SponsorReqType machinery needs no changes, only the name/requirement strings.

## [13] Dashboard standings footer permanently displays the hardcoded lie 'Stage 1 · Rd 9/12'  {prio=medium effort=small}
WHERE: ui-poc/app.html:2536
The DASH_SKEL legend row contains a literal '<span …>Stage 1 · Rd 9/12</span>' (app.html:2536) with no id, and renderDashboard never updates it — it shows 'Stage 1 · Rd 9/12' during Champions in year 5, and the real stage is 11 rounds, not 12. Either wire it to header phase/round data the exporter already sends (current_phase is in every payload) or delete the span; a wrong number in the standings chrome undermines trust in the correct ones next to it.

## [14] Spectated ranked matches are labeled 'Forced Ranked Match' between teams 'Blue' and 'Red'  {prio=low effort=small}
WHERE: cpp/src/SoloQ.cpp:211-217
force_solo_match builds teams named 'Blue'/'Red' and the event string 'Forced Ranked Match' (SoloQ.cpp:211-217), which the match viewer renders verbatim when the player uses the 'Queue Ranked Match' spectate feature. 'Forced' is internal API vocabulary leaking to the player. Minimum fix: pass 'Ranked Match' as the event; better: title it around the prospect being watched ('Ranked — spectating <handle>') and name lobbies with Valorant rank flavor ('Radiant Lobby') derived from host MMR, which the function already computes.

## [15] British/American dialect and punctuation drift across screens  {prio=low effort=small}
WHERE: cpp/src/Team.cpp:194; cpp/src/GameManager.cpp:2036; ui-poc/app.html:3189,6682; cpp/src/SaveGame.cpp:813
Strategy blurb says 'prioritises' (Team.cpp:194) and retirement news counts 'career honours' (GameManager.cpp:2036) while the player-profile section header is 'Honors' (app.html:3189, also 3118, 7359) — the same accolades are spelled two ways one click apart. The interest pane says 'no fee to prise them away' (app.html:6682) amid otherwise American copy ('finalized', GameManager.cpp:2318). The save-gate message uses a plain hyphen 'in progress - finish the bracket first.' (SaveGame.cpp:813) where every other toast uses an em dash. Pick American + em dash as house style and sweep these five spots.

## [16] Label polish: 'Team Chemistry' archetype reads like a stat, and 'Ice-cold Closer' vs 'Ice-Cold Clutcher' casing clash  {prio=low effort=small}
WHERE: cpp/src/Player.cpp:2100, 952 vs 2405
Fifteen of the sixteen rookie archetype names describe a person ('Mechanical Prodigy', 'Clutch Specialist') but TeamChemistryPlayer displays as just 'Team Chemistry' (Player.cpp:2100) — on the god-mode card it looks like a metric, not a player type; 'Locker-Room Glue' or 'Glue Guy' (its own blurb at 2122 already says 'Glue guy') would match the set. Separately the playstyle label 'Ice-cold Closer' (Player.cpp:952) and the archetype 'Ice-Cold Clutcher' (Player.cpp:2405) hyphen-capitalize differently while describing the same trait family — pick one casing. Also in this bucket: the recruitment staff tab's oddly salesy empty state 'No coach on staff — hiring one is an upgrade.' (app.html:5632) vs Market's plain 'No coach on staff' (app.html:5628 region).

# CATEGORY: consistency

## [1] Transfer Market leaks exact hidden potential, bypassing the whole scouting fog-of-war  {prio=high effort=small}
WHERE: cpp/src/DashboardExport.cpp:1553 + ui-poc/app.html:4989
export_market emits the true value: `jw.kv_int("potential", p->potential)` for every free agent (DashboardExport.cpp:1553) and the Market table prints it raw in the POT column (app.html:4989). The SAME player is fogged everywhere else: Recruitment emits pot_lo/pot_hi bands unless scouted (DashboardExport.cpp:3930-3932), and the Player modal gates on potential_known (DashboardExport.cpp:2107-2117). A player can skip scout credits, commissions, and band-tightening entirely by checking the Transfers screen — the scouting economy is dead on arrival for FAs. Fix: apply the same pot_known/scouted_band logic in export_market and render a band in the fapot cell.

## [2] Awards screen is a player-navigation dead-end (no ids exported, no rows clickable)  {prio=high effort=medium}
WHERE: cpp/src/DashboardExport.cpp:1710 + ui-poc/app.html:5808
Every other player list opens the player modal (roster app.html:3043, favorites 3647, watchlist 3733, records 3818, match scoreboards 4039, mail 4525, market 4996, scouting 5282/5307, recruitment 5548, team profile 6203). The Awards screen — a screen entirely about players — has zero click-through: MVP-race rows (5808-5827), recap winners/finalists (5843-5860) and history rows (5871-5877) are inert. Root cause is the contract: export_awards emits only name/team strings, never a player id (DashboardExport.cpp:1710-1717, 1733-1740, 1750-1760, 1786). Add `jw.kv_int("player_id", p.id)` to each emitter and wire recWireRow on the rows.

## [3] Transfer-bid verdict is a client-side guess while Re-sign/Sign verdicts are engine-truth  {prio=high effort=medium}
WHERE: ui-poc/app.html:6427
The Re-sign and Sign tabs get a live engine round-trip (get:resignpreview, app.html:6350-6380) so the accept/fair/reject banner is accurate. The Bid tab reuses the identical rsBanner UI but computes its verdict locally as fee/value ratio (pmBidVerdict, app.html:6427-6434): >=1.15 shows 'Strong bid — the seller is likely to accept.' The engine's actual threshold (Team::sell_threshold_k, Team.cpp:3025-3069) multiplies by strategy (Contender 1.55x) and adds +0.60 for a core piece, so a 1.15x bid on a contender's star reads 'likely to accept' and gets refused every time — same UI element, opposite reliability. Add a host-side bid-preview verb (mirroring resignpreview) that returns will_sell/threshold hints.

## [4] Destructive/costly action patterns are inverted: one-click money spend vs armed confirm for cheap actions  {prio=high effort=small}
WHERE: ui-poc/app.html:6086, 4528 vs 7784, 3272
Team Profile's 'Offer $X' button posts act:buyplayer immediately on a single click at the suggested fee — real budget spent, no arm step, no slider (tpBuyPlayer app.html:6086, wired at 6201). Mail Delete is also instant (4528). Meanwhile Release (7784-7797) and staff Fire (3272-3287) both require the two-step 'Confirm?' armed pattern. The most expensive click in the game has the least protection. Give the Offer button the same armed two-step (or route it into the modal's Bid tab), and consider arming mail Delete.

## [5] Dashboard standings and squad rows are inert while their twins on other screens navigate  {prio=medium effort=small}
WHERE: ui-poc/app.html:2728, 2844
Competition standings rows open team profiles with full click/keyboard/aria wiring (app.html:3380-3384), but the visually-identical dashboard standings rows have no handler at all (2728-2740). Same for players: the dashboard squad card lists name/OVR/RAT/K-D rows (2844-2854) with no openPlayerModal, while every other player row in the app opens the modal. Users learn 'rows are clickable' on one screen and it silently fails on the home screen. Reuse the openProfile/openPlayerModal wiring on both dashboard lists.

## [6] Three money formats for the same budget: '$2500K' vs '$2.5M' vs raw-dollar/thousands protocol mix  {prio=medium effort=medium}
WHERE: ui-poc/app.html:4955, 5153 + cpp/src/DashboardExport.cpp:1538
fmtK (app.html:2483) and the exporter's format_money (DashboardExport.cpp:169-179) both switch to $M above $1M, but Market and Scouting headers hand-roll '$'+Math.round(budget/1000)+'K' (app.html:4955, 5153) so a $2.5M budget shows '$2500K' there and '$2.50M' on Finance/Dashboard KPIs and '$2.5M' on Recruitment (5682) and Team Profile (6149). The protocol itself is mixed: budget is raw dollars in market/scouting/teamprofile exports (DashboardExport.cpp:1538, 3343, 1957) but _k in recruitment/resign preview (3890, 2068), forcing per-screen client patches (6144-6151). Standardize on budget_k in every exporter and fmtK everywhere in the UI.

## [7] Scout-report accuracy tiers disagree between the Scouting screen and the report itself  {prio=medium effort=small}
WHERE: ui-poc/app.html:5094 vs cpp/src/DashboardExport.cpp:2970
The exporter labels confidence Thorough>=80 / Solid>=55 / Partial>=30 / Vague (DashboardExport.cpp:2970-2971) and the report modal displays that string verbatim (app.html:6832). The Scouting screen's completed-reports list recomputes its own chip with different cutoffs: Solid>=60 / Partial>=40 (skAccInfo, app.html:5094). A 57%-accuracy report reads 'Solid' when opened but 'Partial' in the list next to it. Either pass the exported confidence string through to the list, or align skAccInfo's thresholds to 55/30.

## [8] News and Mail dates are off by one day in the identical 'Y·D' format  {prio=medium effort=small}
WHERE: cpp/src/DashboardExport.cpp:483 vs 1295
Mail exports day 1-based: `jw.kv_int("day", m.day + 1)` (DashboardExport.cpp:1295); dashboard news exports it raw 0-based: `jw.kv_int("day", item.day_in_year)` (DashboardExport.cpp:483). Both render as 'Y{year}·D{day}' (news app.html:2865, mail 4479/4499), so the same event dated in both feeds shows e.g. D40 on the dashboard and D41 in the inbox — while the header clock uses the 1-based day_in_year+1 convention (349, 608, etc.). Add +1 to the news emitter.

## [9] Two different OVR color scales: the same OVR reads as a different judgment on Dashboard vs everywhere else  {prio=medium effort=small}
WHERE: ui-poc/app.html:2476-2480
ovrColorDash maps 64→84 to gray→amber→green and ovrColorRoster maps 55→90 (app.html:2476-2480). Dashboard standings/squad and Competition standings use the Dash ramp (2741, 2850, 3404); roster, market, favorites, watchlist, recruitment, team profile, and player modal use the Roster ramp (3031, 4988, 3641, 3724, 5511, 6196, 7185). An 84-OVR player is fully green on the home screen but visibly short of green on his own profile; a 60-OVR is bottom-gray on the dashboard but mid-scale on the roster. Pick one ramp per entity type (team vs player) and share it.

## [10] Same category pill, different colors: 'Result' is blue in News, green in Mail; 'Award' amber vs gold  {prio=medium effort=small}
WHERE: ui-poc/app.html:2466 vs 4424
NEWS_COLOR maps Result→--blue and Awards→--amber (app.html:2466-2470); MAIL_COLOR maps Result→--green and Award→--gold (4424-4425), and Mail's Board is --red — the color that means Upset/Rivalry/danger in the news feed and danger text everywhere else. Both screens render the pills identically (18%-mix chip), so the color is the only semantic carrier and it flips meaning between two adjacent screens. Merge into one shared CATEGORY_COLOR map keyed by category name.

## [11] Team talk is only settable from the Dashboard hero — the Strategy (match-prep) screen has no control  {prio=medium effort=medium}
WHERE: ui-poc/app.html:2786 (only callsite)
act:teamtalk is posted from exactly one place: the dashboard next-match chips, and only when days_until===0 (app.html:2783-2786; sole grep hit). The Strategy screen is the designated match-prep surface (opponent intel, map prep, comp overrides, renderStrategy 4634-4788) yet has no team-talk row, and the cmdbar's Match button (2411-2416) lets a user launch the series from any screen — silently forfeiting the one-shot 1.5-2.5% edge (GameManager.cpp:731-755) unless they happened to route via Home on matchday. Add the ttChip row to the Strategy screen (and/or the pre-match state of the Match screen).

## [12] Two FA-signing flows with different capabilities and no cross-link: Market one-click vs modal negotiation  {prio=medium effort=medium}
WHERE: ui-poc/app.html:4927 vs 7150
The Market row's Sign button posts act:signfa:<id>:market — an instant 'meet their ask' attempt (app.html:4927, 4992) that the engine can still refuse (evaluate_resign_offer path, vlrweb.cpp:576-619), returning only a toast. The Player modal's Sign tab for the same FA offers years/wage/bonus/role sliders with a live engine acceptance preview and counter-offers (7150, 6367-6380, 6521-6543). Nothing on the Market row indicates the richer flow exists one click away behind the small profile arrow. Either make the Sign button open the modal's Sign tab, or label it 'Sign at ask' with a secondary 'Negotiate…' action.

## [13] Dashboard day-rail and standings footer are hardcoded fiction contradicting the Calendar screen  {prio=medium effort=small}
WHERE: ui-poc/app.html:2684, 2536
The day-rail plots four phases at fixed positions [Stage 1 @14%, Regionals @52%, Masters @74%, Champions @92%] over /200 (app.html:2684-2691) while the engine runs an 11-phase calendar where Regionals 1 starts around day ~34 (GameManager.cpp:15-57) — during Regionals 1 the today-dot sits far left of the 'Regionals' tick while the Calendar screen (engine-driven, DashboardExport.cpp:1425-1498) correctly shows Regionals 1 as current. The adjacent standings footer literally always reads 'Stage 1 · Rd 9/12' (static text, app.html:2536) even though stages have 11 rounds and the phase changes. Drive the rail from the calendar export's phase list and either bind or delete the footer string.

## [14] Same number is 'ASK' on Market, 'Wage' on Recruitment, and Recruitment hides any price for free agents  {prio=low effort=small}
WHERE: cpp/src/DashboardExport.cpp:3933-3934 + ui-poc/app.html:5454
For a free agent, Market shows ask_k = fa_current_demand_k under an 'ASK' header (DashboardExport.cpp:1557, app.html:4971/4991); Recruitment exports the identical underlying figure as wage_k = contract.amount_k under a 'Wage' column (DashboardExport.cpp:3934, app.html:5454/5514) and dashes out the Value column for FAs (value_k forced 0 at 3933, rendered '—' at 5502). So the FM-style hub's own 'Free Agents' tab shows no asking price framed as such, and a user comparing screens sees the same $K figure under two contradictory labels. Rename the Recruitment column for FAs (e.g. 'Ask') or add an ask_k field there.

## [15] God-mode affordance gated differently in the two detail modals  {prio=low effort=small}
WHERE: ui-poc/app.html:7164 vs 7020
The Player modal always renders the god-mode toggle chip, even for rivals/FAs where editing is disabled ('always shown so the user can flip it', app.html:7163-7164), while the Staff modal renders the chip only for your own staff (7020) and hides it entirely on rival staff. A user who learns 'god chip lives in the header' from players will conclude staff can't be god-edited (or vice versa, that flipping it on a rival player should do something). Pick one rule — chip always visible with disabled editors, or chip only when editable — and apply to both modals.

## [16] Teams are addressed by name in bridge commands while players are addressed by uint64 id  {prio=low effort=medium}
WHERE: ui-poc/app.html:3382, 6086 + cpp/src/DashboardExport.cpp:3838
Player flows all use ids (nav:playermodal:<id>, act:signfa:<id>). Team flows use raw names: standings rows post go("teamprofile:"+team_name) (app.html:3382), transfer bids embed the seller's name in the colon-delimited command (act:buyplayer:<sellerName>:..., 6086/6423), brackets highlight the user club by name equality (5931), and the exporter resolves team-profile requests and the match viewer's user side by name (DashboardExport.cpp:1903-1919, 3838-3839). Any future duplicate/renamed team name (god-mode rename exists via act:teamset:name) silently misroutes these. Export a team id and switch nav/act verbs to it, mirroring the player convention.

## [17] Stat-header vocabulary and casing drift between tables: 'RAT' vs 'Rating' vs 'RATING', 'K/D' vs 'K-D-A'  {prio=low effort=small}
WHERE: ui-poc/app.html:4019 vs 3008, 4127, 7394
Most tables use ALL-CAPS compact headers — 'RAT'/'K/D' on roster (app.html:3008), favorites (3626), records (3867), team profile (6170), player-modal history (7375) — but the match scoreboard uniquely uses Title Case 'Player/Agent/Rating' (4019), and the series-MVP card labels the same stats 'RATING' and 'K-D-A' (4127) while the dashboard squad card uses 'RAT' and 'K/D' (2851-2852). The player-modal tournament table mixes both in one header row: 'K-D-A' and 'K/D' side by side (7394). Settle on one abbreviation set (RAT, K/D, K-D-A reserved for the kills-deaths-assists triple) and one casing for headers.

## [18] 'Gold' MVP treatment is actually amber in half its uses  {prio=low effort=small}
WHERE: ui-poc/app.html:1684, 1677-1699
The MVP/champion accent is inconsistently sourced: .mmvp uses var(--gold) #FFD700 (app.html:1670) but the series-MVP wrap card uses var(--gold,#d4a14c) with the AMBER hex as fallback (1681, 1687, 1699) and .mvMvpCrest hardcodes an amber gradient #ffd97a→#d4a14c (1684-1685). Since --gold is defined, the fallbacks never fire, but the crest gradient guarantees the flagship MVP card renders amber next to true-gold labels — and amber elsewhere means 'warn/Behind' (TONE map 2461, sponsor pills 3492-3494), muddying whether the card is celebratory or cautionary. Replace the hardcoded crest gradient and fallbacks with the --gold token.



# APPENDIX A — VERIFICATION VERDICTS (18 adversarial checks)

### dash-legend — CONFIRMED
app.html:2535 contains the literal legend "Playoff line · top 4" and 2536 has a static span "Stage 1 · Rd 9/12" with no id; grep shows those strings occur nowhere else in the file (no JS template writes them, .stfoot appears only in CSS at 285-287 and this static markup). GameManager.cpp:620-634 builds regional playoffs by sorting teams on phase_wins and taking the top min(8, size) into an 8-team double-elim bracket, so the playoff cut is top 8, not top 4 — the legend is wrong as claimed.

### expiring-invisible — PARTIALLY-TRUE
The zero-occurrence claim is false: case-insensitive grep for 'expir' in C:/Users/fulls/Desktop/Valosim/ui-poc/app.html hits line 7 (<meta http-equiv="Expires">) and line 7281, where the player detail modal renders ct.exp_year with an "Expires" label — so a UI surface does show contract expiry, though it does not highlight/warn on expiring contracts. The roster-table part is accurate: at line 3035 the contract renders as a plain cell `<span class="rcontract">${yrs}y · ${sal}</span>` with no conditional styling for years_left, even though nearby cells (e.g. rating at 3033) do get conditional colors.

### market-quicksign — PARTIALLY-TRUE
Confirmed: ui-poc/app.html:4927 marketSignFA posts "act:signfa:"+faId+":market" directly on button click with no confirmation step, and cpp/src/vlrweb.cpp:577-604 do_signfa defaults (years=2, role=-1, bonus=0, amount_k=-1) meet the player's full ask (amount = probe.ask_k) and on refusal call fa->register_rejected_offer (line 603). Wrong detail: register_rejected_offer (cpp/src/Player.cpp:3515-3532) does NOT durably raise the ask in this path — it only applies a mood hit when the offer ratio is below 0.85 of the ask, and the quick path offers ratio ~1.0 (tier 0, mood_hit 0.0, no-op); even when a hit applies it is a mood bump that decays across offseason days (decay_mood), not a durable price increase.

### fav-watch-unreachable — CONFIRMED
In C:/Users/fulls/Desktop/Valosim/ui-poc/app.html: renderFavorites (line 3608) and renderWatchlist (line 3693) are fully implemented, and the nav-active function at line 7983 selects fav=$("#navFav"), watch=$("#navWatch"). The nav rail markup (lines 2392-2417) contains buttons navHome through navCal plus navNew, but no #navFav or #navWatch elements. A whole-file grep for go('favorites') and go('watchlist') returns zero hits; the only entry points are hash/route dispatch (lines 8184-8194) and host bridge messages (lines 8005-8006, 8112-8113), so the screens are unreachable from the UI without hand-typing a hash or a host push.

### mail-cap-40 — CONFIRMED
DashboardExport.cpp:1291 breaks the items loop at 40 ("show the latest 40") while GameManager.cpp:3022 sets kMailCap = 150 in push_mail, so up to 110 older messages can exist in mailbox but never be exported. Line 1285 exports "total" as gm.mailbox.size() (the true count), and app.html:4455-4458 displays that M.total in the mail screen header ("N messages" / "N read"). All parts of the claim match the code.

### bid-reject-toast-only — CONFIRMED
In GameManager.cpp, user_transfer_bid (~lines 3850-3921): the will_sell rejection branch sets only o.message = "<seller> rejected the bid — they value <player> at about $<asking_k>K." with no mail_user call (nor do the roster-guard or window-closed rejection branches), while the Accepted branch calls mail_user(...MailCategory::Transfer, "Signing complete: ...") creating a mail item. In app.html showToast (~6258-6273), the toast fades via setTimeout at 2600ms and removes at 2600+300ms, matching the ~2.6s auto-fade claim.

### fonts-not-loaded — CONFIRMED
app.html line 12 loads exactly Space Grotesk 500;600;700, Inter 400;500;600, IBM Plex Mono 500;600. Body font is Inter (--font-ui, line 53), and .klab (line 204), .pill (line 257), and sticky header .rhead (line 351) all set font-weight:700 with no font-family override, so they render Inter at an unloaded 700 weight. font-weight:800 appears at .mmLogo (line 124, Space Grotesk max 700), .piBadge (line 1366, IBM Plex Mono max 600), .mvMvpName (line 1688), .ngCrest/.ngTitle (lines 2161-2163), plus an uncited extra at line 2198 — all weights never loaded, so browsers synthesize the bold.

### ghost-contrast — CONFIRMED
In app.html :root, line 19 has --bg-2:#1C202C and --bg-3:#242938, line 23 has --fg-ghost:#7C8499, exactly as claimed. Independent WCAG computation from the hex values gives 4.347:1 on #1C202C and 3.876:1 on #242938, matching ~4.35 and ~3.87 and both below the 4.5:1 AA threshold. The token is indeed used on tiny labels: .ovrbox .l 8.5px (line 382), .stat .l 8px (387), .cbox .l 8px (408), .pmStat .l 8px (1207), all with color:var(--fg-ghost).

### award-grammar — CONFIRMED
GameManager.cpp:4923 builds the mail subject as aw.winner->name + " named " + cat + " of the season!" inside the shared add_award lambda, whose comment (4918-4919) says it fires for MVP and every role-of-the-year award. Categories passed as cat include "Duelist of the Year" (5004), "Initiator/Controller/Sentinel of the Year" (5006-5010), "IGL of the Year" (5024), "Rookie of the Year" (5078), and "Flex of the Year" (5117), yielding subjects like "X named Duelist of the Year of the season!". Claim matches exactly, including line numbers.

### wins-vs-win — CONFIRMED
GameManager.cpp line 4042 sets the news headline as champion->name + " wins " + tour.name() (singular), while line 4063 sets the champion mail subject as "CHAMPIONS — " + champion->name + " win " + tour.name() + "!" (plural). Both refer to the same tournament victory, so the verb agreement is inconsistent exactly as claimed.

### market-potential-leak — CONFIRMED
DashboardExport.cpp: export_market_to_json (line 1503) emits `jw.kv_int("potential", p->potential)` at line 1553 for every free agent (up to 40), with no scouted check anywhere in the function. The recruitment exporter at lines 3919-3932 gates it: `bool pot_known = p.potential_scouted || on_user;` then emits exact `pot` only if known, else `pot_lo`/`pot_hi` from `gm.scouted_band`. app.html line 4971 declares a POT column header and line 4989 renders `${esc(String(fa.potential))}` raw, so the FA market page reveals true hidden potential and bypasses the scouting economy for FAs.

### awards-no-ids — CONFIRMED
export_awards_to_json (DashboardExport.cpp:1671-1796) emits only name/team/role/country strings for MVP race candidates (lines 1710-1717), award winners (kv_str winner_name/winner_team, 1733-1740), finalists (1750, 1758), and history (1786) — no kv_int of any player/coach id anywhere in the function. In app.html the awards renderer (race rows ~5808-5832, recap cards ~5843-5860, history rows ~5871-5877) builds pure innerHTML with no addEventListener/data-pm/openPlayerModal wiring, whereas other player lists throughout the app open the modal (e.g. lines 3647-3648, 3818-3819, 5282-5283, 5307-5308, 5548-5549).

### mail-news-day-off — CONFIRMED
DashboardExport.cpp:1295 exports mail day as `jw.kv_int("day", m.day + 1)` while :483 exports news day as `jw.kv_int("day", item.day_in_year)` raw. Both values come from the same 0-based GameManager::day_in_year (0..199): MailItem.day is documented as "GameManager::day_in_year (0..199)" (GameManager.h:459) and news items are assigned `n.day_in_year = day_in_year` (GameManager.cpp:936 etc.). app.html renders both in the same format — news at line 2865 (`Y${n.year}·D${n.day}`) and mail at lines 4479/4499 (`Y${it.year}·D${it.day}`) — so an event on the same in-game day shows D(n+1) in mail but D(n) in news.

### no-prize-money — CONFIRMED
The finished-tournament block in play_tournament_round (GameManager.cpp:948-993) and force_finish_stale_tournaments (516-594) only push news, record trophies/finals appearances, capture qualifiers, and log H2H finals — no budget mutation anywhere. A grep for "prize" in cpp/src finds no crediting code, only the projection comment (7349-7351), a UI help string in gui_manager.inc:636 claiming "prize winnings refill it" (false in code), and an unrelated Match.cpp comment. Meanwhile project_team_finances (7340-7360) books prize_k = 120/40/15 by tier into projected_income_k (7351-7352). All actual budget credits (sponsorship 1769, stipends 834/6466, promotion 6512, transfers) are non-prize, so the projected prize income is never realized.

### phase-skip-no-guard — CONFIRMED
vlrweb.cpp lines 131-136: the "phase" branch of do_advance loops step() (which calls g_gm->advance_day) up to 80 times, breaking only when current_phase() changes — no check for a pending or played user match, unlike the "match" branch (line 129) which breaks on the user's phase_wins+phase_losses changing. app.html lines 2413-2415: the Continue/Match/Phase cmdbar buttons call advance('day'|'match'|'phase') directly, and advance() at line 8023 just posts "advance:mode:screen" with no confirmation or days_until check, even though the dashboard payload exposes next_match.days_until===0 (used elsewhere, e.g. lines 2700, 2767). So one Phase click on a matchday sims the user's own series (and any playoff/Champions matches within the current phase) off-screen; the only mitigation is that recordings are captured into g_lastUserMatch/g_lastUserSeries for after-the-fact viewing.

### teamtalk-one-site — CONFIRMED
Grep over ui-poc/app.html finds "act:teamtalk" only once, at line 2786, inside the dashboard hero's team-talk chips which are rendered only when nm.days_until===0 (ternary at lines 2774-2780). renderStrategy at line 4634 contains no team-talk control (no teamtalk/team_talk references between 4634 and its callers at 7998+). In cpp/src/GameManager.cpp, user_team_talk is consumed at lines 731-737 and reset to 0 at line 755 after the user's match, so entering matchday without setting it forfeits the bonus.

### storyline-3-variants — CONFIRMED
At C:/Users/fulls/Desktop/Valosim/cpp/src/Match.cpp lines 2574-2586, the storyline logic is an if/else-if/else chain: a special-case bomb line for MVP with >=30 kills ("[!] %s drops a %d-bomb!"), then when OVR gap > 10 either "MASSIVE UPSET BY <name>!" or "Domination by <name> as expected.", else the fallback "A closely fought tactical matchup." So the generic pool is exactly the three claimed variants (upset, domination, fallback), plus the bomb special case the claim allowed for.

### two-ovr-ramps — CONFIRMED
app.html:2476-2480 defines ovrColorDash with t=(o-64)/20 (64-84 range) and ovrColorRoster with t=(o-55)/35 (55-90 range), identical color stops but different normalization. The dashboard squad card applies ovrColorDash to player OVR at line 2850 (also 2741, 3404), while the roster table (3031), player modal (3140, 7185, 7271-7274), free agents (4988), market (5511, 6196), and other views apply ovrColorRoster to the same player OVR. E.g. OVR 74: Dash gives t=0.5 (full gold), Roster gives t≈0.543 (past gold toward green) — same value, different color per screen.


# APPENDIX B — SUBSYSTEM ROUGH EDGES (from the 10 reader agents)

# MAP: game-manager — GameManager (world orchestration / sim loop) — C:/Users/fulls/Desktop/Valosim/cpp/src/GameManager.cpp + C:/Users/fulls/Desktop/Valosim/cpp/include/GameManager.h
PURPOSE: GameManager is the god-object that generates and runs the whole esports world: 3 regions x 3 tiers of leagues plus solo-queue ladders, an 11-phase 200-day season calendar, and the daily advance loop that sims AI matches, captures the user's series for the live viewer, and fires progression/news/mail. It also owns all year-end machinery (awards, retirements, Hall of Fame, AI re-sign/poach markets, staff markets, promotion/relegation, finance) and every user-facing management system (scouting fog-of-war/briefs/assignments, board objectives, sponsors, mail inbox, transfer bids, staff hire/fire, team talk, auto-manage).
ROUGH EDGES:
- transfers_open() is a hardcoded `return true` stub (GameManager.h:574); its own comment admits the AI offseason market (B8 walk-poach, ai_full_offseason_pass, lower-tier) and run_mid_season_replacements are NOT wrapped, so a future transfer-window feature currently gates almost nothing (GameManager.h:568-576).
- Stale header docs on world_difficulty: GameManager.h:177-180 and h:1061-1065 say it is 'not consumed by the engine yet', but it is actively consumed at GameManager.cpp:719-720 (match strength), cpp:1766 (user economy), cpp:2403 (AI star retention), cpp:3120 (board ambition), cpp:6719 (AI scouting sharpness).
- Stale header doc on budget tiers: GameManager.h:163-164 documents Rich=$3M/Contender=$2M/Mid=$1.5M/Budget=$0.8M/Expansion=$0.5M but the implementation uses $2M/$1.2M/$0.8M/$0.4M/$0.25M (GameManager.cpp:472-476).
- News dedup keys use player_key(name + country_iso) 'because we don't have a numeric player_id exposed' (GameManager.cpp:5671-5673) — but Player::id exists and is used for mail deep links; a same-name same-country pair would collide, violating the identity-by-id invariant in spirit.
- MVP doc comment vs code drift: the formula block at GameManager.cpp:4947-4950 documents sos = 1.15 attended / 1.10 finals, but mvp_score_breakdown implements 1.08 attended / 1.15 won-intl (cpp:4324-4326).
- DayResult.matches_simulated is hardcoded to 18 on stage days and 4 on tournament days rather than counted (GameManager.cpp:1203, 1221).
- MailItem doc says 'In-memory only (no save/load exists)' (GameManager.h:436) while SaveGame is friended for private world state at h:148 — one of the two comments is stale; the mailbox's persistence status is ambiguous.
- The NewsItem header comment lists only 5 categories (GameManager.h:417) but the code emits ~15 (Tournament, Breakout, Slump, Hot Streak, Milestone, Rumor, Rivalry, Upset, Historic, Dynasty, Retirement Watch, Prospect, Promotion, Relegation, Result) — UI category filters keyed to the doc would miss most items.
- Stat sanity sweep clamps career_matches to 5000 and career_kills to 250000 at year-end (GameManager.cpp:1917-1934) — a symptom guard for the historical duplicate-stat bug rather than a fix; silent clamping could mask a regression.
- run_coach/scout/analyst_market count hires and poaches but discard them with `(void)hires; (void)poaches;` (GameManager.cpp:6937, 7264, 7337) — no log/news emitted for AI staff churn beyond the per-hire log lines.
- Non-wizard initialize_world leaves the staff FA pools empty until the first year-end market, so a career started via that path has an empty staff market for season 1; only initialize_world_with_config seeds 12 per region (GameManager.cpp:495-506).
- Wizard rename intentionally leaks the displaced procedural team name ('The displaced name simply leaks', GameManager.cpp:400-404).
- board_ambition_ is computed and stored (GameManager.cpp:3121) but never read at verdict time — judge_board_objective judges purely on placement + the objective list; the 'verdict tone' use promised at GameManager.h:484 is unimplemented.
- next_user_match_day_in_year returns day_in_year ('today or soon') for any tournament where the user is in the current round (GameManager.cpp:1659-1668) — an acknowledged approximation that can mislabel the countdown UI.
- emit_rivalry_match_night's dedup key embeds day_in_year (GameManager.cpp:6048-6050), so the same pairing can generate a rivalry item every meeting once h2h>=4 — heavier feed pressure than other one-per-year emitters; its comment also notes the tournament path is 'stubbed'.
- fa_current_demand_k is a trivial passthrough of contract.amount_k kept only as a future extension seam (GameManager.cpp:4003-4008).
- Power rankings only credit tournament CHAMPIONS — 'No accessor for finalist/SF this year' is an acknowledged TODO (GameManager.cpp:5382-5383).
- compute_power_rankings' win_rate is the season tally used as a 'proxy for last 5 series' because no sliding window exists (GameManager.cpp:5360-5363).
- run_bracket_to_completion resolves series ties by 'higher seed (a) wins' on an ambiguous map count (GameManager.cpp:6492) — silent seed bias in Ascension/relegation brackets.

# MAP: player — Player subsystem (cpp/src/Player.cpp + cpp/include/Player.h)
PURPOSE: Player.cpp/Player.h define the Player entity for the VLR Manager sim: procedural generation (attributes, nationality, four coexisting personality/playstyle systems, IGL/flex overlays), monthly age-curve progression with Monte-Carlo potential, and the whole economic layer — hidden 1-10000 reputation, contract/wage generation, transparent re-sign negotiation scoring, transfer value — plus career bookkeeping (awards, per-tournament stats, mastery, mood/discontent/restlessness). It is the single largest state object; Match.cpp, Team.cpp, GameManager.cpp and the web UI all read/write through it.
ROUGH EDGES:
- Dead code: PerfBuckets/compute_perf_buckets (Player.h:1106-1121, Player.cpp:1674-1699) is defined but never called anywhere in cpp/; the big comment block at Player.cpp:1646-1672 still describes performance-targeted growth ('a player who got 80 first-bloods grows entry/aim') that the actual progression no longer does — progression is pure age-curve + noise. new_rounds is computed then (void)-discarded at Player.cpp:1816-1817.
- evaluate_resign_offer's tenure ramp reads the global current_world_year() (Player.cpp:3284) and computes the ask with current_year=0 (Player.cpp:3178); before GameManager first sets the global (g_world_year=0, Player.cpp:1008) tenure counts every salary_log entry and gen_contract's exp falls back to year 0.
- 'Recent trophy' in evaluate_resign_offer treats ANY career [T]/[M]/[W] on any roster player as recent — acknowledged hack in comment at Player.cpp:3249-3255 ('rely on ... simple presence ... until tenure-time tracking matures').
- desire_accepts compares offers against contract.amount_k, the stored ask that decay_demands() erodes daily (Player.cpp:2919-2921, 652-657) — can drift far from what gen_contract would currently ask.
- Two acknowledged duplicated blocks that can drift: the canonical IGL stat shift exists in generate_player (Player.cpp:3656-3667) and again in apply_rookie_archetype::TacticalIGL (2154-2165), and the mental composite is inlined at 2183-2190 mirroring mental_score() at 2345-2355.
- Award/year matching is substring-based throughout (a.find(year_str) in gen_contract:401-407, best_season:1326-1328, career_timeline:1249-1251) — an award containing any other 4-digit run, or a team name with digits, can false-match a year.
- pick_spawn_desire computes career_trophies via trophy_summary() on a freshly generated player — always 0 at spawn, so the 'RingChaser: no ring && ovr>=70' bonus (Player.cpp:2806) degenerates to an OVR>=70 check; the trophy proxy only matters if the picker were ever re-run mid-career (it isn't).
- should_retire ignores its current_year parameter entirely ((void)current_year, Player.cpp:3799) and consumes rng — fine on the sim path but a latent determinism trap if ever called from a UI/preview path.
- Reputation doc says '1..10000' (Player.h:354) but the spawn seed clamps to 9600 (Player.cpp:3769) while drift clamps to 10000 (1453) — cosmetic inconsistency.
- mood_breakdown playtime_mod hands out -12 whenever season_matches < 4 (Player.cpp:593), so every player on every team reads as unhappy in the early weeks of each season before sample accrues.
- kast() falls back to an over-counting estimate (K+A+survivals+trades summed per round) for saves where the binary tracker was never populated — acknowledged at Player.cpp:235-245.
- Handle de-dup appends suffixes cumulatively on repeated collisions ('Name' → 'Name42' → 'Name4278') and never inserts the final handle back into the taken set within the function (Player.cpp:3550-3562) — relies on external registration.
- Ctor rolls contract_years = irange(1,4) (Player.cpp:109) but the placeholder contract generated at ctor end (133-134) derives its own years — the contract_years snapshot and contract.exp_year can disagree for never-signed FAs (mostly harmless since years_left keys off exp_year).
- monte_carlo_potential burns 20 sims x ~120+ monthly ticks of the ONE global RNG per deep-potential spawn (Player.cpp:3831-3861) — deterministic but makes generation-order part of the RNG stream contract; also its copies mutate trajectory_class/ovr_at_last_tick on the copy only, fine, but the copy of a shared_from_this type by value is a subtle pattern.
- awards_seen_ hash mirror is bypassed by any direct awards.push_back (badge/dynamic paths push to badges not awards, but check_dynamic_badges-era external code was the concern) — header admits 'those paths should be migrated to add_award()' (Player.h:1098-1102).

# MAP: team-staff — Team / AI club brain + staff (Coach, Scout, Analyst) + game-agent data (Agent.cpp) + GOAT scoring (Goat.cpp)
PURPOSE: Team.cpp/Team.h is the club brain of the sim: it holds every org's finances (budget, wage envelope, wealth tier, sponsor deal, transfer log), long-term identity (strategy with inertia, championship window, brand tag, org memory, chemistry graph), and all AI roster behavior — cutting, signing, scouting targets via score_scout_target with an FM-style reputation gate, buying/selling with fees and sell thresholds, and per-map comp/agent selection at match time. The staff files define three hireable staff roles whose numeric attributes feed concrete multipliers: Coach (match synergy, player development, roster-building lean), Scout (fog-of-war reveal, potential-band accuracy, AI discovery), Analyst (opposition-report depth, bounded per-map prep tilt). Note: Agent.cpp is NOT a staff role — it is static VALORANT game data (29 agents, 11 maps, 4 comps, 30 badges); Goat.cpp is the GOAT-leaderboard scoring formula, also not staff.
ROUGH EDGES:
- run_spend_fork is declared with a full doc comment (Team.h:504-509, 'Workstream A4: rich Contender BUYS... poor Rebuilder banks cash') but has NO implementation anywhere in the codebase — grep hits only the header. Dead declaration; the advertised strategy+wealth spend fork does not exist as a Team method.
- Stale docs around finals: Team.h:658-660 and Team.h:699-700 still say finals_history_ is an unwired hook and ChampionEra/Contender tiers are 'unreachable', but record_finals_appearance IS called at GameManager.cpp:562-563 and 975-976 — the comments are out of date, the tiers are live.
- Stale doc in team_import_appetite: header comment Team.h:756 claims region multipliers 'Americas 1.10 / EMEA 1.00 / Pacific 0.85' but the implementation uses 1.05 / 0.90 / 0.75 (Team.cpp:455-457, 'STRICT v2').
- ai_full_offseason_pass upgrade restart is off-by-one: after committing a cut+replace it does 'slot = 0; continue;' (Team.cpp:2264) but continue triggers ++slot, so the rescan actually starts at slot 1 and slot 0 is never re-checked. Currently harmless because kMaxUpgradesPerPass = 1 breaks the loop first (Team.cpp:2192-2195), but the restart logic is dead/wrong if the cap is ever raised.
- scout_top_fas (Team.cpp:1116-1159) is a legacy parallel signing path: it scores with the random cosmetic scout_prefs (rolled 1-10 at construction, Team.cpp:62-73), counts roles over the WHOLE roster instead of the starting 5, has no role-fit or reputation gates, and doesn't pin roles — duplicated, inconsistent logic vs auto_fill_roster/score_scout_target.
- cuts_this_year_ is incremented by EVERY release_player call including natural contract expiries (Team.cpp:841 + ai_manage_roster releasing yrs_left<=0 players), so org-memory stability_culture punishes clubs for letting contracts lapse the same as firing — 'hire/fire culture' conflated with normal churn.
- Inconsistent money model on signing: sign_player never debits budget (score_scout_target comment (d) at Team.cpp:1046 says the caller must), but resign_player debits one full year's wage immediately (Team.cpp:2938) AND pay_payroll charges the wage again annually; ai_full_offseason_pass also pre-debits ask_k (Team.cpp:2258) while auto_fill/scout_top_fas debit get_transfer_value instead — three different up-front-cost conventions.
- Chemistry map is keyed by raw Player* pointers (ChemKey/ChemKeyHash Team.h:100-111) — fine in-memory, but save/load must remap pointers (identity invariant is uint64 id); whether SaveGame serializes/rebinds the chemistry map was not verified in this pass and deserves a check.
- update_org_memory tenure is an admitted approximation: tenure_proxy = min(salary_log.size(), age-16, 6) (Team.cpp:2447-2455), so 'signed within 3 years' / 'age at signing' can be wrong for players with gaps; the import 'cut mid-season' detection is likewise a self-described best-effort heuristic that can't actually see cuts (Team.cpp:2499-2505).
- team_import_appetite can't see the world year, so 'recent international appearance (last 2 years)' degrades to 'any [M]/[W] award ever on the roster' — acknowledged in-comment (Team.cpp:380-386). Award/trophy detection throughout Team.cpp (import appetite, org memory, trophy_case) is substring matching on award strings ('[T]', 'Masters', year-as-substring), which is brittle against name format changes.
- score_scout_target and decide_contract_years both take current_year but immediately (void) it (Team.cpp:993, Team.cpp:2023) — the parameter is threaded through every callsite and unused ('reserved for future budget-projection use').
- Scout and Analyst personalities are explicitly cosmetic: 'bias generation only... they do NOT yet drive AI discovery flavour' (Scout.h:11-12, Analyst.h:11-15) — StarChaser/BargainHunter/AntiStratSpecialist etc. only shift spawn stats, no behavioral hook exists.
- Stale doc in Team.h:534-535: enforce_one_igl comment says 'Controllers/Initiators preferred, Sentinels OK' but the implementation is a flat 1.13 multiplier for all three non-Duelist roles (Team.cpp:619-630, which explains the flattening — the header was never updated).
- Cut logic is copy-pasted between ai_manage_roster (Team.cpp:1987-2017) and ai_full_offseason_pass Stage 1 (Team.cpp:2122-2156) — thresholds and chemistry-protection must be kept in sync by hand.
- Coach/Scout/Analyst carry BOTH contract_years (decremented yearly per Coach.h:63) and contract_exp_year, the opposite of the player model where contract_years is a frozen snapshot and exp_year is live (Team.cpp:527-531) — two contract conventions coexist.
- A backup source file cpp/src/gui_main.cpp.bak2 is sitting in the source tree and still matches greps (e.g. match_synergy_mult hits) — stale artifact that pollutes searches.

# MAP: match-sim — Match engine (round-by-round sim), Series (BO2/BO3/BO5), and series JSON export
PURPOSE: Match.cpp is the core round-by-round Valorant map simulator: it picks per-map agent comps, runs a duel-by-duel kill loop with dozens of bounded attribute/archetype/phase multipliers, tracks economy and sides, and finalizes per-player VLR-style ratings plus career/season/per-tournament stat writes. Series.cpp is a thin BO2/BO3/BO5 wrapper that tallies map wins, aggregates per-player stats, records each map as an immutable RecordedMatch for replay/live viewing, and applies league W/L on finalize. MatchExport.cpp is an output-only, hand-rolled JSON serializer (schema v2) that turns a vector of RecordedMatchPtr into a full series export (per-map player lines, per-round history, series totals, MVP) for the file-export feature and smoke tests.
ROUGH EDGES:
- No map pick/ban or veto system exists: every series driver draws each map uniformly at random from maps(), so the same map can repeat within one BO3/BO5 (GameManager.cpp:707-708, Tournament.cpp:485, gui_match.inc:27-28) — despite CONTEXT mentioning map picks, none are modeled.
- RoundLog.spike_planted/end_kind is deterministic hash-synthesized flavor (Match.cpp:2129-2164) and can contradict the real in-round spike_planted flag that drove PostPlant phases and KillEvent.post_plant (Match.cpp:970, 1819-1829) — a replay's kill feed and its round-end label can disagree.
- Identity-by-pointer, not uint64 id: RoundEvent, HistoryRecord.stats, match_stats, and MatchExport's player_agg are all keyed by raw Player* (Match.h:81-137, MatchExport.cpp:445), so recordings are session-only; series-MVP selection iterates an unordered_map keyed by pointers, so exact tie-breaks are address-order dependent (MatchExport.cpp:509-518).
- Round-40 hard cap resolves marathon overtimes with a raw 50/50 rng coin flip regardless of team strength (Match.cpp:848-858).
- Genuine non-zero map ties are 'resolved deterministically for safety' by always crediting team1 (Series.cpp:40-42); comp_tag_for also defaults to DoubleInitiator when no role has 2+ picks (Series.cpp:59-63).
- Event classification is fragile substring matching on event names, with inconsistent case handled by double-checking both "MASTERS"/"Masters" and "CHAMPIONS"/"Champions" in at least four separate places (Match.cpp:169-176, 503-505, 1650-1655, 2466-2484) — a renamed event silently loses pressure/intl/nerves effects.
- tourn_identity_key's first_digit reads only the FIRST digit of the label (Match.cpp:68-71), so a hypothetical "Stage 10" or "Masters 10" would mis-bucket; safe only while stages stay 1-9.
- BO2 support (Series.cpp:18-23, MatchExport 'tied' winner at 524) appears vestigial: the current tournament code builds group stages as BO3 explicitly because 'a draw never happens in VCT' (Tournament.cpp:406-409).
- MatchExport must INFER best_of from the final score because the replay path never retains the configured Series::best_of_ (MatchExport.cpp:527-545); a forfeit-shortened or abandoned series can still mislabel series_type.
- MatchExport.cpp header comment still reserves economy_log, utility_impact, heatmap_kills, ability_usage, timeline_events for v2+ (MatchExport.cpp:6-8) while the exporter_notes admit 'utility_impact not tracked' and ACS is always an estimate with acs_estimated:true hardcoded (MatchExport.cpp:372, 552-556).
- Agent-name string literals hardcode game knowledge throughout the duel loop: info agents Cypher/Sova/Fade (Match.cpp:541-561), lurker agents Yoru/Viper/Cypher (Match.cpp:1615-1624), smoke controllers Brimstone/Omen/Clove (Match.cpp:1938-1942), plus eight per-agent flavor branches (Match.cpp:743-802) — adding an agent requires touching all of these.
- Forfeit maps (roster <5) end 13-0 with zero rounds, empty round_history and all-zero stats (Match.cpp:250-262), so the live viewer timeline for such a map is empty and detect_overtime/players arrays in the export are degenerate.
- Match ctor caches chosen_agents_ from both teams into one map and comp_synergy/comp_tag_for must reverse-lookup roster membership with an O(roster) inner loop per entry to split it back per team (Match.cpp:236-237, 421-448; Series.cpp:47-64) — awkward and quadratic-ish, flagged by comments as bug-prone (Bug 2 note at Match.cpp:1927-1936).
- kv_bool/kv_raw and arr_int exist in JsonWriter but several helpers (e.g. kv_raw) appear unused in this file; the writer trusts callers for validity of raw fragments (MatchExport.cpp:128-134).

# MAP: tournament — Tournaments, brackets, Masters/Champions, and the SoloQ prospect ladder (Tournament.cpp/.h, SoloQ.cpp, plus GameManager tournament lifecycle)
PURPOSE: Tournament.cpp is the self-contained bracket engine: it runs group stages, single-elim, and VCT-style double-elim brackets (BO3 everywhere except BO5 LB Final and Grand Final), accumulates per-event player stats, and pins championship award strings onto players. GameManager owns the lifecycle: an 11-phase season calendar (3 stages, 3 regionals, 2 Masters, Champions, awards, offseason) spawns tournaments into `active_tournaments`, plays one bracket round per matchday on the daily tick, and force-finishes leftovers at phase boundaries. SoloQ.cpp is the per-region ranked ladder (~900 generated prospects each) that simulates daily MMR matches with balanced-lobby matchmaking, doubles as the free-agent fill pool for team rosters, and feeds the prospect-scouting/leaderboard surfaces.
ROUGH EDGES:
- Forced-champion fallback: if schedule_next_bracket_round finds nothing to schedule, it declares a champion and sets Phase::Done (Tournament.cpp:640-649) — but award_event_titles is only called from the SingleElim and GrandFinal branches of play_round (Tournament.cpp:798, 854), so this path crowns a champion with NO player titles pinned and an empty gf_history_, which validate_bracket_state flags as invalid (Tournament.cpp:932-936).
- No tournament prize money exists anywhere: champions get a trophy and news but zero budget; the only match income is a flat $10K/team per tier-1 stage matchday (GameManager.cpp:834) and $5K tier stipend (GameManager.cpp:6466). Yet the finance projection charges teams a 'prize expectation' of 120/40/15 $K by tier (GameManager.cpp:7349-7352) — projected income that can never materialize.
- Tournament wins have no direct prestige effect either — prestige only moves at season end from aggregate W/L (GameManager.cpp:1906) and promotion/relegation (GameManager.cpp:6513, 6524); 'prestige' consequences of winning Champions are indirect (trophy case, power rankings).
- Event identity is name-substring based, contra the id-not-name invariant: award kind sniffs name_.find("Regionals"/"MASTERS"/"CHAMPIONS") and silently skips awards on no match (Tournament.cpp:297-307); capture_regional_qualifiers and tournament generation gate on the same substrings (GameManager.cpp:597, 620, 652). Renaming an event family silently kills titles.
- Stale/contradictory comments about group-stage format: code makes groups BO3 (Tournament.cpp:408-409) but resolve_groups_to_playoffs still says 'BO2 ties are allowed' (Tournament.cpp:423-426), play_round handles a 'BO2 tie: winner is null' case (Tournament.cpp:693-694), and both Tournament.h:286 and Tournament.cpp:939-940 claim group stage is BO1.
- Only 3 regions exist — kRegions = {Americas, EMEA, Pacific} (cpp/include/Common.h:193); no China league despite the project brief listing 4 regions, so Champions is a 12-team field, not 16.
- Tier 2/3 (Challengers) leagues have no tournaments at all — regionals draw only from tier-1 `leagues` (GameManager.cpp:624-632); lower tiers get only the round-robin play_tier_stage_round (GameManager.cpp:1206) and promotion/relegation.
- Group-stage matchday interleaving is wrong-ish: play_round plays the next groups_.size() matches from a matchup list built group-by-group (Tournament.cpp:399-412, 657-661), so a 'round' is N consecutive matches of the SAME group — group A completes before group B starts, not one match per group per day.
- Tournament::play_series calls auto_fill_roster(fa) without the year argument (Tournament.cpp:482-483) while GameManager's series path passes `year` for contract exp_year stamping (GameManager.cpp:701-702) — inconsistent call convention on the same fill API.
- balance_lobby's controller redistribution is asymmetric: only the t1-has-3+-controllers case is handled; a triple-controller t2 is never rebalanced (SoloQ.cpp:133-135).
- force_solo_match's comment claims it takes 'the 9 nearest-MMR players regardless of region' (SoloQ.cpp:191-192) but the pool is this engine's per-region ladder_ — cross-region spectating depends entirely on the caller picking the right engine (vlrweb.cpp:460).
- force_finish_stale_tournaments clears active_tournaments unconditionally after its safety-64 loop (GameManager.cpp:524-537, 593); a bracket somehow needing >64 rounds would be silently discarded with no champion, no news, and no qualifiers captured.
- record_trophy double-record risk on the force-finish path is acknowledged in-comment as delegated to 'Agent B's storage layer' dedup (GameManager.cpp:550-556) — the idempotency does exist in Team::record_trophy (Team.h:671-675), but the comment reads as an unresolved handoff.
- REGIONALS pacing gives 9 matchdays for a ~7-9 round 8-team double-elim (GameManager.cpp:48-51) — tight enough that force-finish can still silently complete late rounds off-screen, the exact 'playoffs never happened' feel the comment says was fixed.
- Tournament.h:77-79 says the default ctor format is DoubleElim, and BracketMatch.best_of defaults to 3 with byes recorded as played matches with a null b and stamped best_of — harmless but bye rows look like real matches to naive UI consumers (Tournament.cpp:732-738).

# MAP: export-contract — DashboardExport (engine→UI JSON data contract)
PURPOSE: DashboardExport.cpp is the single serialization layer between the C++ engine and the WebView2 UI: 23 exporter functions that turn GameManager/Team/Player state into hand-rolled JSON (own JsonWriter, no library, DashboardExport.cpp:51-159). Every exporter is OUTPUT-ONLY by contract — no mutation, no RNG consumption — which is what keeps reads dynasty-deterministic (DashboardExport.cpp:1-13). vlrweb.cpp:146-165 dispatches each screen name to its exporter; three more (resign preview, player interest, scout report) are invoked on-demand from act: verbs at vlrweb.cpp:751/762/771.
ROUGH EDGES:
- last_5_form emits empty-string opponent and score on every entry — real W/L only; documented as fabrication-avoidance pending a team-level match log (DashboardExport.cpp:528-546).
- budget_trend is a single-point array (current budget in $K) because no budget history is persisted; a documented follow-up ring-buffer (548-556).
- Regions hardcoded as Americas/EMEA/Pacific in five separate places: competition view.regions (974-976), staff-profile kAllRegions (2733), scouting coverage (3364), newgame options (3437), recruitment (3895). Engine agrees (GameManager.cpp:378 rejects other regions; China is only a country under Region::Pacific, Country.cpp:84) — but this contradicts the stated design of a China region, and adding a 4th region would silently break all five lists (staff-profile lookup would miss employed staff there entirely).
- Tier-2/3 competition views ship an empty schedule by construction — lower divisions have no exported fixture slate, leaving those screens standings-only/data-starved (1021-1024).
- Identity-by-name violations of the uint64-id invariant: export_team_profile resolves teams by name (1903-1919, user-team preference only mitigates self-collision), player-profile club lookup is by team_name string (2054-2060), player_interest seller lookup by name (3507-3510), and match export detects user side by name equality (3838-3839).
- Exported-but-unused fields (0 hits in app.html): per-player discontent (719), joined_year/titles_with_org (801-802), watchlist status_name (2472-2473), kill-feed t1_alive/t2_alive (3777-3778), team-object committed_payroll_k/net_transfer_k raw values (667-669; the KPI cards carry the displayed versions).
- export_newgame_options ignores gm entirely ((void)gm, 3428) — regions/tiers/difficulty are hardcoded, and the tier array order carries a load-bearing comment 'MUST match NewGameConfig::OrgTier' with no static check (3439-3447).
- Team profile history rows carry only year/wins/losses (2008-2017) and awards history only year/category/winner_name/is_coach (1777-1789) — noticeably thinner than the rest of the contract; team pages feel data-starved on placements/trophies.
- Coach has no quality field in roster staff export because the engine lacks Coach::quality01, unlike Scout/Analyst — UI must fall back to reputation (comment at 840-841, 843-857).
- Recruitment exports the entire world player pool with no cap (3898-3947) and recomputes every brief's match count against that pool per export (3970-3972) — O(players x briefs) on every screen refresh; records similarly walks the full population (documented heavy, header 133-136).
- Scout-report duration estimator duplicates GameManager::commission_scout's formula inline (est lambda, 2935-2939) — the two can drift apart silently.
- Chemistry pairs dereference raw Player* that may be off-roster mid-season; safe only under the never-free invariant, with an explicit do-not-prune warning comment (814-820).
- player_interest blocker text prints Y.get_transfer_value()/1000 with %d (3614-3615) — int truncation is fine today but overflows if transfer values ever exceed ~$2.1B in raw dollars.
- Magic display caps scattered inline: mail 40 (1291), transfers 20 (1226), market FAs 40 (1545), staff pools 12 (1605), admirers 8 (3658), awards history 40 (1778), schedule 16/18 (1026/1481) — no shared constants.
- Mixed day-basis in mail: day emitted +1 (1295) but assignments/day math elsewhere raw; and calendar emits day_in_year twice (header 1451 and body 1455).

# MAP: bridge-save — vlrweb bridge host + SaveGame slot system (with gui_main legacy status)
PURPOSE: vlrweb.cpp is the WebView2 host that owns the single live GameManager session and acts as the backend for ui-poc/app.html: it receives colon-delimited command strings (get:/nav:/advance:/act:), mutates or queries the world through existing engine paths, and replies with PostWebMessageAsJson state/toast messages. SaveGame.cpp/.h implement the v1 binary multi-slot save/load (whole-world serialization keyed by stable uint64 ids, with an active-tournament refusal gate) plus a cheap header-only peek for the slot list. gui_main.cpp is the older ImGui/DX11 frontend built as a separate vlrgui.exe.
ROUGH EDGES:
- safe_screen (vlrweb.cpp:177-186) and export_screen (vlrweb.cpp:144-166) are two hand-maintained parallel lists of the same 19 names; adding a screen requires editing both or nav silently falls back to dashboard.
- The autosave 'quiet' branch swallows ALL failures including the tournament gate (vlrweb.cpp:941 — '/* autosave: silent either way */'), so a player who advances through a whole bracket gets no autosaves and no warning; the sole visible gate message only appears on manual saves.
- build.bat's completion banner still calls vlrgui.exe 'the real game' and vlrweb.exe 'the web migration' (build.bat:134-135), directly contradicting launcher.cpp:6-8 which declares vlrweb the PRIMARY frontend — a stale label, and vlrgui.exe still costs a full compile of the 7568-line gui_main.cpp every build.
- user_reputation_block duplicates the club-stature formula from Team::score_scout_target by hand (prestige*85 + wealth_tier*500 + 600, vlrweb.cpp:674-677 per its own comment); if the engine formula changes the gate silently diverges.
- do_signfa's 'meet their ask' path calls evaluate_resign_offer twice — a probe at 0 salary just to read ask_k, then the real evaluation (vlrweb.cpp:594-599); relies on the ask being deterministic per the comment but is a fragile double-evaluation pattern.
- g_signfaReason is a mutable global used to smuggle refusal text from do_signfa to the toast (vlrweb.cpp:576); fine single-threaded but easy to leave stale.
- All state/toast JSON is built by raw string concatenation with a minimal json_escape that drops every control char below 0x20 except newline (vlrweb.cpp:189-197); safe today because payloads come from exporters, but the wrapper itself trusts screen names.
- act:newgame arity is fixed at 8, so a team name containing ':' would shift every argument; the UI is expected to strip colons (same assumption documented for setname at vlrweb.cpp:286-287) but the host does not defend newgame the way it rejoins teamprofile/watchnote tails.
- do_newgame clamps OrgTier to 0..4 and hardcodes starting_year = 2026 (vlrweb.cpp:535-541).
- SaveGame's now_stamp uses non-thread-safe std::localtime (SaveGame.cpp:177) — harmless single-threaded, worth knowing before adding background saves.
- The reputation-gate refusal message hardcodes the pronoun 'his' for every player (vlrweb.cpp:679).
- resolve_ids silently drops unresolvable ids for most collections (rosters, favorites, HoF — SaveGame.cpp:160-173, 1442-1444), meaning a subtly damaged save can load 'successfully' with quietly missing players; only league team lists hard-throw (SaveGame.cpp:1539-1543).
- read_player re-asserts saved attributes after the ctor because generate_badges may have re-applied badge mods (SaveGame.cpp:453-457) — a correctness-critical ordering note that any Player ctor change could break.
- The v1 gate message mixes punctuation styles: SaveGame.cpp:813 uses a plain hyphen ('progress - finish') while every toast in vlrweb uses a UTF-8 em-dash.
- The cpp/ directory is littered with 300+ stale build/test log files (build.log through build_wsb*.txt etc.) checked into the working tree alongside sources.

# MAP: ui-a — app.html lines 1-2900 — CSS design system, shell markup (nav rail/cmdbar/modals), screen skeletons, and renderDashboard()
PURPOSE: This region is the entire visual foundation of the single-file WebView2 UI: a ~2,388-line CSS design system (tokens plus per-screen namespaced component styles for ~20 screens), the static shell markup (nav rail, command bar, empty #view main, two modal shells), and the first chunk of JS (bridge helper, color/format helpers, four screen skeleton template strings, and the full renderDashboard implementation). Every screen the C++ engine pushes state for is styled here; the JS render functions later in the file only assemble markup from these classes.
ROUGH EDGES:
- Spacing tokens are dead: --s1..--s7 (8pt scale) declared at app.html:34 but `var(--s#)` appears 0 times in the whole file — every gap/padding/margin is a hardcoded px value, so the '8pt' system is aspirational only.
- Duplicate conflicting rule: .scard .sfoot .sfoot-right defined twice — app.html:423 (display:flex; gap:7px) and app.html:1831 (display:inline-flex; gap:9px); the later one silently wins.
- Undefined token used: .skDonerow .dn .dt at app.html:2054 sets font-family:var(--font-text) but no --font-text exists (:root only defines --font-display/--font-ui/--font-num) — falls back to inherited font, likely a typo for --font-ui.
- Dead/incomplete rule: .sch.user::before at app.html:487 is `{content:"";position:absolute}` with no dimensions/background — compare .srow.user::before (:275) which draws a 3px bar; looks like an abandoned copy.
- Exact duplicate rule: .ff .empty (app.html:341) repeats the shared .empty (:214) declaration-for-declaration.
- Font weights used but not loaded (app.html:12 loads Space Grotesk 500/600/700, Inter 400/500/600, IBM Plex Mono 500/600): font-weight:800 on .mmLogo:124, .piBadge:1366 (mono!), .mvMvpName:1688, .ngCrest:2162, .ngTitle:2163, .ngStart:2198; weight 700 on --font-num elements (.recrank:1537, .mvNums:1633, .skCovbig:2070 etc.) and on Inter-default labels everywhere — all browser-synthesized faux-bold.
- Gold/amber confusion: the fallback idiom var(--gold,#d4a14c) at app.html:1677,1681,1687,1699 uses the AMBER hex (#D4A14C) as fallback for --gold (#FFD700), and .mvMvpCrest (:1685) hardcodes an amber gradient (#ffd97a,#d4a14c) under a 'gold' MVP label — the Series-MVP card is actually amber while other MVP accents are true gold.
- Hardcoded soft-red #ff8a94 appears ~20+ times (e.g. :258,:313,:364,:476,:592,:900,:1222,:1387,:1828,:1832,:2115,:2325,:2351) as the de-facto 'danger text' color but was never promoted to a token; likewise the medal silvers/bronzes #C8D0DC/#D69A5C (:1591) and side-tint literals #6ea8ff/#ff8087/#eaf9ff/#ffeaed (:1664,:1753,:1798) bypass the token system.
- Transition style drifts by section age: older sections enumerate properties with var(--t-fast) var(--ease); newer ones hardcode .1s-.18s (e.g. .cswPill:448 .12s, .rcRow:2310 .1s, .mvMapTab:1657 .15s, .ngCrest:2162 .18s) or use `transition:all` (.rsRole:1891, .ngSeg:2175, .mact:2236, .spChoose:2217, .rcStHire:2386), which older code deliberately avoided.
- Mobile media queries are unreachable-by-design: body has min-width:1240px (:48) yet the file carries max-width breakpoints at 560/620/640/680/720/760/820/1080px (:1228,:2203,:1816,:1240,:2232,:2279,:2364,:1977) — in a narrow window the viewport-based queries fire while the layout is still forced to 1240px, a contradictory state.
- Hardcoded placeholder in DASH_SKEL: the standings footer span 'Stage 1 · Rd 9/12' (app.html:2536) has no id and renderDashboard never updates it — it displays this fixed text regardless of actual phase/round. Same for skeleton defaults 'Omega Esports' and '95 / 200' (:2493,:2498), though those ARE overwritten (:2659,:2662).
- Day-rail phase positions are hardcoded percentages (["Stage 1",0.14],["Regionals",0.52],["Masters",0.74],["Champions",0.92], app.html:2684) and the 200-day year is a magic number repeated at :2662,:2664,:2685 — nothing comes from engine state, so if the C++ calendar shifts, the rail lies.
- Massive intentional duplication as architecture: at least 13 comment blocks say classes are 'uniquely-named so they never collide' (:743,:782,:822,:879,:947,:1044,:1413,:1454,:1504,:1970,:2152,:2260,:2283) — the flag chip alone exists as .favflag:1431/.wlflag:1468/.recflag:1542/.reclflag:1592/.skFlag:2004/.rcFlag:2319/.pmFlag:1146 with subtly different padding/size each time; consistency drift is guaranteed and later reviewers should diff these families.
- Micro-label font sizes are a near-continuum — 8, 8.5, 9, 9.5, 10, 10.5px all mean 'small uppercase label' with letter-spacing anywhere from .03em to .14em (e.g. .stat .l 8px:387, .ovrbox .l 8.5px:382, .pill 9px:257, .rhead 9.5px:351, .klab 10px:204, .plate 10.5px:178) — no tiered scale; row-text similarly splits hairs between 12.5/13/13.5px and table row heights span 30/32/34/36/38/40/42/44/46/48/50px with no rhythm.
- id/purpose mismatch: the main-menu nav button is id=navNew (app.html:2409, presumably once 'New Game') but is titled 'Main Menu' and calls showMainMenu(); the .cmdbar is position:fixed (:94) yet nested inside <nav> markup (:2412), relying on nav's padding-bottom:158px (:65) to avoid overlap.
- Google Fonts loaded from CDN (app.html:10-12) in a shipped offline WebView2 app — first run without network silently falls back to system fonts, changing the entire look.

# MAP: ui-b — ui-poc/app.html lines 2900-5700 — screen render functions (roster/god-mode through recruitment hub)
PURPOSE: This region is the per-screen render layer of the single-file WebView2 UI: each renderX(data, club) function wipes #view, injects a screen skeleton template, populates it from the host-pushed JSON state, and wires interaction handlers that post colon-delimited act:/nav: commands back to the C++ host. The universal pattern (documented at 2903-2912) is no optimistic DOM updates — every write posts an exact bridge command and relies on the host to mutate, re-push fresh state (full re-render), and toast; module-scoped variables (godMode, selectedPlayerId, mailOpenId, recTab, rcState) preserve UI selection/filters across those full rebuilds.
ROUGH EDGES:
- app.html:3382 — Team profile navigation is by NAME (go("teamprofile:"+t.team_name)), and the match wrap-up MVP match at 4106 is also name-based (a.name===S.series_mvp) — both fight the stated identity-by-uint64-id invariant.
- app.html:4528 vs 3272-3287 — Inconsistent destructive-action patterns: mail Delete fires immediately with no confirm, while staff Fire requires a two-step armed confirm.
- app.html:4953-4956 vs 5682-5683 — Three budget unit conventions in one region: Market's M.budget is raw dollars (formatted manually as $..K), wage_envelope_k is $K via fmtK, and Recruitment's header uses budget_k/wage_room_k; Scouting (5153) repeats the raw-dollar manual format.
- app.html:5002-5063 vs 5615-5648 — Fully duplicated staff-hire UI: Market's 'Hire Staff' card and Recruitment's 'Staff' tab both post act:hirestaff with parallel row markup and separate empty-state copy — one is presumably vestigial.
- app.html:4316-4321 — buyTier() hardcodes economy thresholds that 'mirror Match.cpp's invest_to_tier' — engine constants duplicated in JS with no single source of truth; will silently drift if the engine changes.
- app.html:5335 — renderNewgame's region fallback is ["Americas","EMEA","Pacific"] — omits China even though the game context lists four regions (only matters if the host omits ng.regions).
- app.html:3619,3704,4499,5167 etc. — esc() misused inside textContent assignments (e.g. $("#readerStat").textContent=esc(it.category)) — a category or count containing '&' would display literal '&amp;'; harmless for numbers but a latent double-escape pattern.
- app.html:3596,3678,3782,3993 — FAV/WL/REC/MATCH skeletons hardcode crest 'O' and their renderers never set #hcrest/#navCrest or populate #hmeta, unlike every core screen; renderScouting (5151) hardcodes crest 'S' and skips #navCrest entirely.
- app.html:4587-4588,4928,4933-4934,5322,5429,5736 — Dev/test hooks leaked onto window (__stratSetPrep, __stratAgentAuto, __marketSignFA, __marketHireStaff, __scouting, __newgame, __recruitment) — inconsistent (only some renderers get them) and look like leftover scaffolding.
- app.html:3765-3766 — Watchlist note text posted verbatim mid-command (act:watchnote:<id>:<text>:watchlist) — a note containing ':' relies entirely on host-side tail-rejoin logic that must also strip the trailing screen token; fragile compared to the colon-stripping done for club names (3003, 5416).
- app.html:4774 — Comp editor 'dup race → revert' re-posts the unchanged original comp to the host — a wasted write round-trip (full state re-push + re-render) used as a revert mechanism.
- app.html:4482-4486 — Clicking an unread mail only posts act:mailread and relies on the host re-push to actually open it in the reader; if the push is delayed/lost the click appears to do nothing (read mails open locally via openItem).
- app.html:4849 — Calendar fill line hardcodes the track inset as calc((100% - 68px) * frac) with a comment 'track runs left:34px → right:34px' — magic numbers coupled to CSS defined elsewhere.
- app.html:5729 — Recruitment infinite scroll re-runs the full rcFiltered() filter+sort over the whole exported pool on every scroll event just to read .length.
- app.html:4170 — total_rounds_any() is the lone snake_case function in an otherwise camelCase file; minor style inconsistency in renderMatch.
- app.html:3033,3113-3114,3294,4003 — Negative/danger color is the hardcoded literal '#ff8a94' repeated across roster ratings, trajectory, mood, and chemistry instead of a design-token var (the positives use var(--green)/var(--amber)).
- app.html:3134 — Morale meter is a fully inline-styled bar (width:84px;height:6px;background:var(--bg-3);border-radius:3px;overflow:hidden;display:inline-block) grafted onto the .pt class — layout in markup fighting the stylesheet.
- app.html:5086-5087,5331 — More hardcoded palette: skCovColor lerp stops (#5A6172/#D4A14C/#3FD17A) 'reuse the same stops as attrColor' by copy-paste, and NG_COLORS is 8 raw hexes; neither references CSS vars.
- app.html:3630,3806,5102 — 'Flag' cells (CTRY columns, skFlag, recFlag) render raw uppercase ISO text with an em-dash fallback — placeholder-grade country display used across five screens.
- app.html:2934,4056-4060 — No transfer/FA *negotiation* UI exists in this region despite the game's contract/negotiation systems: Market signing is one click at ask_k and Recruitment offers no bid flow (watchlist status 'Bidding' at 3691 is a label only).
- app.html:3115 — Honors title list silently truncates to the first 8 titles with no 'and N more' indicator.

# MAP: ui-c — ui-poc/app.html lines 5700-8333 — Awards/Brackets/TeamProfile screens, Player & Staff detail modals (negotiation + god mode), toast, routing/onState, standalone fallback, main-menu title screen, boot
PURPOSE: This region is the back half of the single-file WebView2 UI: three read-only world screens (Awards MVP races, playoff Brackets, rival Team Profile), the two deep overlays (Player Detail modal with re-sign/sign/bid negotiation, interest scans, scout reports and god-mode editing; Staff Profile modal), and the global toast. It also holds all app plumbing: the onState message router with its transient-screen intercepts, the go/advance bridge commands, the bridge-less bundled-data fallback, the client-rendered main menu with save slots and settings, and boot with hash deep-linking. NOTE for reviewers: renderMatch (line 4046), renderMail (4429), and renderCompetition (3320) are NOT in this region — they live earlier in the file.
ROUGH EDGES:
- render(screen,data,club) at app.html:7994 is dead code — never called anywhere (verified by grep); onState (8100-8126) and renderFromFallback (8134-8230) each re-implement the dispatch with subtly different payload shapes, so three copies can drift.
- onState sets worldActive=true, removes menuMode, sets currentScreen and nav BEFORE the dispatch if-chain (8096-8099); a state push with an unrecognized screen name still exits the title screen and highlights Home while leaving #view stale.
- Interest/Scout staleness: pmInterestAsked/pmScoutAsked reset only when a DIFFERENT player id renders (7111), so closing and re-opening the SAME player after advancing days repaints the cached payload and never refetches — pmInterestCache/pmScoutCache (6318, 6329) are never invalidated for the whole session.
- God-mode attribute/potential steppers capture `base` from the render-time payload (7946-7961): every click before the host re-push posts the same base±1 value, so rapid clicking cannot step more than 1 per round-trip.
- Team identity by NAME in this region violates the project's id-not-name invariant: brackets highlight the user via string equality isUser (5931), and act:buyplayer embeds the seller's verbatim team name in the colon-delimited command (6086, 6423) — comment at 6421 merely asserts team names are colon-free.
- Comment drift: pmSignSubmit is described as "the 8-arg signfa" at 6638 but "the 7-arg signfa" at 7878; the actual command has 7 colon-delimited fields.
- pmResignPredictHTML verdict-class logic has a dead branch: `if(verdict==='FAIR' && willAccept) bcls='accept'` (6472) can never fire because willAccept already selected 'accept' in the first condition (6470).
- Player-modal god-mode chip is always shown "so the user can flip it" even on non-editable players (7163-7164), while the staff modal only shows it for own staff (7020) — inconsistent gating between the two modals.
- mmLatest picks the newest save via lexicographic localeCompare on saved_at strings (8241) — only correct if the host emits sortable (ISO-like) timestamps.
- mmRenderPanel falls back to hardcoded slots [{slot:1},{slot:2},{slot:3},{slot:auto}] before get:saves returns (8292), so Save buttons can be clicked against phantom slot metadata.
- renderBrackets motion helper assigns ids to .tourn cards and schedules .in adds, but the cards never carry the .enter class so only the header actually animates — acknowledged half-fix in the comment at 6031-6032.
- showToast collapses every level except "warn" to "ok" (6262) — no error/info tiers despite the host sending an arbitrary level string.
- Team-profile finance units are inconsistent at the protocol level: budget arrives in RAW dollars while payroll_k/sponsorship_k are in $K, patched client-side with Math.round(budget/1000) (6144-6151).
- FA sign wage slider is hardcoded min 10 / max 180 $K (7667) and the ask seed clamps to 180 (7642) — an elite free agent whose real ask exceeds $180K can never be offered his asking wage.
- Bid pane: feeMax formula (7713) can exceed the user's budget, and the submit button is never disabled — the over-budget state is only flagged by the client verdict text; comment says "One bid per window" but no client-side lockout exists after a bid (7738).
- Escape closes both modals simultaneously when a player modal is opened on top of a staff modal — each document-level keydown listener checks only its own open flag (6949, 6967).
- boot() includes bare "teamprofile" in the known-hash list, posting a parameterless get:teamprofile (8318-8325) that the host must tolerate.
- Scout item tags are hardcoded male: 'best of his kit' / 'for his level' (6845) regardless of the player.

