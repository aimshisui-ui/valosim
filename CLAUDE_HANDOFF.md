# VLR Manager / Valosim — Claude Handoff

Living handoff so no requested item is lost across context windows. Full engineering
history lives in `C:\Users\fulls\.claude\projects\C--Users-fulls\memory\valosim-project.md`
(read it first — it has build cmd, invariants, and Increments 1–18).

## Essentials
- Project: single-player VALORANT esports-manager sim. C++17/MSVC engine + WebView2 web UI (`vlrweb.exe` driving `ui-poc/app.html`). Launcher = `Play VLR Manager.exe`.
- Build: `cpp\build.bat` by ABSOLUTE path, feed EOF via `Start-Process -RedirectStandardInput <emptyfile>`; ~2–4 min; NEVER build while vlrtest/vlrweb running (build-folder write-lock).
- Verify UI: `build\vlrweb.exe --shot <png> <screen> [--cmd <cmd> [--cmd2 <cmd>]]`. Capture GOTCHAS: (1) kill `vlrweb,msedgewebview2` + wait 3s between captures (zombie webview locks user-data-dir → hang, no PNG); (2) `--cmd` needs ~13–20s wait (two chained 3s timers); (3) headless WebView2 = prefers-reduced-motion:reduce.
- Smoke: `build\vlrtest.exe` → must print "ALL TESTS PASSED". Invariant/property tests, NO golden files; `--dynasty` is diagnostic-only. DYNASTY-DETERMINISM: new user-only queues must no-op when empty; wizard uses `initialize_world_with_config` (has preseason buffer), default/test/dynasty uses `initialize_world` (no buffer).
- User is on a WIZARD career (custom team/region), NOT default-world Omega Esports.

## SHAKEOUT-3B (this batch — 2026-06-28). Status: ALL 13 DONE + VERIFIED (real-host captures)
1. [x] **Own players' scout report FREE + instant.** Verified capture: own player Scout tab shows THOROUGH + "Your player · complete report" + full strengths/weaknesses instantly, no commission. ALSO: potential now shows the EXACT value for own players in the scout report (was a fog band) — `p.potential_scouted || own_player` in the report's potential block.
2. [x] **Staff contracts clipping.** `.rstrip` min-height + #view.roster scroll. Verified.
3. [x] **Finance spacing.** `.spCard` margin-top. Verified capture (Nova wizard career).
4. [x] **Staff member profiles.** BUILT+VERIFIED. `export_staff_profile_to_json(gm,id)` (searches ALL tiers' head staff + free + retired pools; one generic modal for coach/scout/analyst via role tag + attrs[]) + `nav:staffmodal:<id>` (g_profileStaffId; "staffmodal" in export_screen AND safe_screen — the review workflow caught the safe_screen omission) + #smModal UI (reuses pm* shell; Hire button for FAs → act:hirestaff). Click-wired: roster staff strip (ids added to roster export staff), recruitment Staff rows (data-st-open), market smrow. Verified: Les Levesque modal (attr bars, career, contract, REP box).
5. [x] **Staff free agents at world gen.** Verified: new career Day 1 → "HIRE STAFF · 36 available" (12/role/region, wizard path, dynasty-safe).
6. [x] **Choose role when signing** — already existed in the player-modal Sign tab ("Assigned role" picker → act:signfa role param). Confirmed in code.
7. [x] **God-mode edit name.** Verified capture: GOD MODE ON → ✎ → name becomes an input, Enter commits act:setname.
8. [x] **Write/maintain this handoff** (STANDING INSTRUCTION — always write a handoff after completing a task set).
9. [x] **Other-region standings.** BUILT+VERIFIED. `export_competition_to_json(gm, view_region="", view_tier=0)` + `view{region,tier,user_region,user_tier,regions[],tiers[]}` + `nav:competition:<region>:<tier>` (persistent g_compRegion/g_compTier; reset in do_newgame) + region/division pill switcher (⌂ home markers) in renderCompetition. Verified: EMEA Challengers standings viewed from an Americas career.
10. [x] **Tier-2/3 teams.** Division pills VCT/Challengers/Open Circuit; lower tiers DO simulate (verified: Riptide 5-0 … Echo 0-5 real records); tier≥2 shows "no fixtures — off-week" (fixtures are tier-1-only by design); rows click through to team profiles.
11. [x] **Per-tournament player stats.** BUILT+VERIFIED. `tournament_stats[]` exported in player profile (from Player::tourn_stats — Stage+Regionals of a split share one bucket, each Masters/Champions separate, newest year first); "By Tournament" table in the modal Detail pane. Verified: "Americas Split 1 · Y2026 · 13 maps · 307-224-5 · 1.37 K/D · 1.21 RAT · 216 ADR · 59% KAST".
12. [x] **Interest tab scrollable.** Verified (Brink5 capture).
13. [x] **PLAYER REPUTATION (FM-style, hidden 1..10000)** — BUILT + smoke green + verified. Player::reputation seeded deterministically (no rng) from OVR/age/potential in generate_player; drifts slowly each season in save_history_and_progress (lags reality — trophies via accolade_score + form; 0.22/yr catch-up). GATE in Team::score_scout_target (e): club stature = prestige*85 + wealthtier*500 + 600 (same 1..10000 axis); a club only pursues players with reputation <= club_stature + 500 → tier-3 can't chase tier-1 stars, and interest shrank (verified: 84-OVR "Star" 34→24 pursuing, +11 admiring). Influences get_transfer_value (×0.7..1.5) + gen_contract wage (×0.85..1.35). Negotiation refusal: user_reputation_block gates user FA signings (toast "won't negotiate — reputation too low"). Export rep_tier band ("Global Icon/Superstar/Star/Established/Rising/Prospect/Unknown") in player-profile + interest JSON; stature chip in modal subheader + "Stature: X" line in interest tab. Smoke green ([85] interest-sync stays valid since export+scorer both gated; salary-band + RNG-sensitive tests within tolerance). TUNABLE: the reach constant (Team.cpp gate `+500`, vlrweb `+900`) controls how many suitors a star draws — lower it for fewer.

## SHAKEOUT-4 (2026-06-28) — ALL DONE + VERIFIED (captures)
A. [x] Scout tab scroll — ROOT CAUSE: #pmContent had no CSS (plain block flex-item overgrew .pmCard's 85vh; overflow:hidden clipped tall tabs). Fix: `#pmContent,#smContent{display:flex;flex-direction:column;min-height:0;overflow:hidden}` → .pmBody scroll chain works on EVERY tab.
B. [x] Scouting notes v3 — PER-PLAYER UNIQUE: `pick(salt,{...})` chooses among 2-3 phrasings per rule via player-id hash (stable, no rng); identity "Profile:" line (dominant cluster × tempo × archetype name); signature-agent note; live numbers (rating, age) in deep reads; character reads (consistency/work-ethic/ego/greed/loyalty) gated accuracy>=65; caps 10/8/5/3/2 by accuracy; id-keyed ROTATION of which notes survive the cap. Verified: hazeka got "aim-heavy Duelist… classic 'Duelist Diva'" + "The Jett is the identity" + "matches say 1.21".
C. [x] Staff god-mode — `act:staffset:<id>:<field>:<value>:<screen>` (do_staffset: role attrs by key, reputation, salary 10-500K, years 0-5 w/ exp_year restamp, name; USER team only). Staff modal: god chip, attr −/+ steppers (attrs now export `key`), salary/years/rep steppers, rename ✎. Verified capture.
D. [x] Team god-mode — `act:teamset:<field>:<value>:<screen>` (do_teamset: name w/ full wizard-rename semantics incl. tag/colors/logo reseed + roster/staff team_name stamps — NOTE uses roster-membership so collision-safe; budget $0-100M in $K; prestige 0-99). Roster screen: CLUB · GOD MODE bar (Name/Budget/Prestige/Apply) under KPIs, visible only in god mode. Verified capture.
E. [x] Mail "Mark all read" — `act:mailallread:<screen>` + pill button in Inbox chead (hidden at 0 unread). Verified capture.
F. [x] AI GM toggle restored — engine flag `user_auto_manage` (smoke [24]); `act:automanage:<0|1>:<screen>`; `auto_manage` exported in strategy JSON; Strategy screen top bar "AI General Manager" w/ Enable/Take Back Control + ACTIVE pulse. Verified capture.

## SHAKEOUT-5 (2026-07-02) — 6 bug reports. Status at handoff-write: BUILT, smoke pending
1. [~] **Can't god-edit head scout/analyst** — ROOT CAUSE: wizard rename stamped only head_coach's team_name; scout/analyst kept the procedural club name → staff modal read them as "not your club". FIXED: wizard stamps all 3 + export_staff_profile's on_user_team is now IDENTITY-based (matches user_team->head_* ids, immune to stale names — fixes existing careers too).
2. [~] **Duplicate player nicknames** — ROOT CAUSE: make_handle() had NO dedupe (the used-name registry existed only for real names) + only 92 roots → birthday-paradox dupes at 2700 players. FIXED: kRoots 92→~275, kSuffixes 12→28, kSecondWords 14→38; global g_used_handles registry w/ 24 retries + numbered fallback ("Frost2"); reset via reset_handle_cache() in reset_name_caches(). NOTE: retries consume extra rng deterministically — fine (no golden tests), but the world equilibrium shifts vs older builds.
3. [~] **Can't re-sign >2 years (score stuck at 54)** — ROOT CAUSE: desire_length_pref buckets (Mercenary/Greedy/RingChaser-on-contender → max 2 yrs; neutral → 3) + a years HARD BLOCK that clamped total to threshold-1 (=54) so max money/bonus could never help. REBALANCED: buckets +1 (shortest personality tolerates 3, neutral 4, Loyal/Stability 5) and the hard block is now a steep SOFT penalty (-20 at +1 over, -34 at +2) that a strong package can buy through at +1. propose_resign_offer max = preferred+2 to match.
4. [~] **FA insta-sign → NEGOTIATION** — do_signfa now evaluates the offer with evaluate_resign_offer (same math as re-sign: wage vs ask, years, prestige, contender, personality, role fit) and can REFUSE (reason toasted via g_signfaReason; register_rejected_offer on refusal). New verb shape act:signfa:<id>:<years>:<role>:<bonus>:<amount>:<screen> (7-arg legacy + 4-arg quick-sign = "meet their ask" — still evaluated, can refuse). Negotiated wage overrides contract.amount_k after sign_player. UI: Sign tab now "Negotiation" — wage slider (10-180K), live accept/refuse verdict via the resignpreview channel (export_resign_preview resolves FAs from the ladders now), counter chip ("Meet $XK/yr"), longshot styling on the submit.
5. [~] **Duplicate TEAM names (user's name vs generated club, e.g. two Mirages in brackets)** — ROOT CAUSE: wizard rename never checked the pyramid for an existing club with the typed name (generator itself is globally deduped). FIXED: GameManager::resolve_user_name_collision() — the typed name is exclusive; any same-named generated club (any region/tier) gets a fresh take_team_name() identity + team_name stamps + reseeded colors. Called from wizard rename + god-mode teamset name.
6. [~] **"No weaknesses" on 65+ OVR scout reports** — ROOT CAUSE: weakness gate was absolute (attr <= 52). FIXED: strengths/weaknesses are RELATIVE to the player's own mean (±9) with the top/bottom 2 ALWAYS surfacing (scout Haaland → still get weaknesses); `relative` flag exported + "for his level"/"best of his kit" chip in the UI.

## MEGA-BATCH (user order, 2026-07-02) — THE ACTIVE WORK QUEUE. Keep updated.
## ⭐⭐ AUDIT BATCH A (2026-07-02, second session) — CURRENT STOPPING POINT
STATE: ~14 engine fixes + ~28 UI fixes from `AUDIT_INSIGHTS_2026-07-02.md` are IMPLEMENTED and the engine BUILDS CLEAN. Smoke initially failed on [27] (rshare 53% vs >0.55) — ROOT-CAUSED as an order-fragile test, NOT a regression: the suite seeds once at main, [27] never reseeded, and prize money legitimately changes career RNG consumption in tests [22]-[26], re-rolling [27]'s random rosters (its comment claimed a symmetric A/B measurement the code never did). Fixed [27] to hermetic multi-seed (0xC0FFEE/1337/42 × 80 maps, save/restore engine — [14b]/[18] precedent, band unchanged).
✅ SMOKE: **ALL TESTS PASSED** (verified before session end — rebuild exit 0, hermetic [27] passes, [28] unaffected). Batch A is code-complete + built + smoke-green.
▶ NEXT SESSION, IN ORDER:
1. **Capture-verify** (the ONLY remaining Batch A step): dashboard (day-rail phases + footer + top-8 legend + mail badge + clickable rows), market (POT bands + Negotiate), mail (filter pills + armed delete), competition (zone markers), strategy (team-talk chips), awards (blurb rows). Plus a matchday capture for the armed sim-guard and the kill-feed NvN/CLUTCH lines. Then checkpoint fablize G004 (Desktop\Valosim\.fablize\).
2. Then **M2 TRANSFER MARKET** (below) — audit findings features#1 (sell-side offers) and features#8 (windows) contain the implementation seams.
WHAT LANDED (details in memory Inc-25): market potential fog (pot_known/pot_lo/pot_hi — UI branches on it), news day +1, mail cap 150, unread_mail + phases[]/season_days in dashboard export, team_talk in strategy header, star_players ids, award-grammar fix, "win" plural, PRIZE MONEY (Champions 400/180K, Masters 250/110K, Regionals 120/50K; Tournament::prize_paid guard; Team::budget is RAW DOLLARS ×1000LL; both finish paths), bid-reject mail w/ asking price, farewell mail for user retirees, storyline variant pools (deterministic salt), "Ranked Match" naming; UI: real font weights, WCAG labels, --red-text token, nav Favorites/Watchlist + unread badge, matchday armed guard + Match→viewer + Space/M/1-9 keys, mail filters + armed Delete + aligned pill colors, openPlayerModal(id,tab) deep links everywhere, Rescan buttons + advance() cache clearing, expiring-contract tints + count, clickable dashboard rows, real day-rail/footer/legend, competition zone markers, strategy talk chips, top-modal-only Escape, armed Offer, MVP blurb row, recruitment scroll cache, kill-feed NvN + CLUTCH, 4 crest fixes, CSS hygiene (dup rules, --font-text, .sch.user::before).

## ⭐ SESSION CLOSE-OUT (2026-07-02) — READ THIS FIRST NEXT SESSION
STATE: **M1 (save/load/menu/settings/slots/maximized) + M3 (live K/D, wrap-up, team talk) are DONE, BUILT, SMOKE-GREEN (ALL TESTS PASSED) and capture-verified.** Everything is committed to the working tree (no git commits — repo only has the old May baseline; don't commit unless asked).
AUDIT: **`AUDIT_INSIGHTS_2026-07-02.md`** (repo root) — 106-finding full-codebase insight report (16 mapper/insight agents + 18 adversarial verifiers; 16/18 hard claims CONFIRMED). Categories: UI changes (16), features (18), QoL (18), visual polish (20), flavor text (16), consistency (18). Top confirmed quick wins: Market leaks TRUE hidden potential (DashboardExport.cpp:1553 → kills scouting economy for FAs); hardcoded dashboard lies ('Stage 1 · Rd 9/12' app.html:2536, 'top 4' legend vs real top-8 cut, fake day-rail ticks app.html:2684); Favorites/Watchlist screens unreachable (no nav buttons); award-mail grammar "…of the Year of the season!" (GameManager.cpp:4923); no prize money but phantom $120K/$40K/$15K projection (cpp:7349-52); font weights 700/800 used but never loaded (app.html:12); mail capped at 40-of-150 in export. Feature list overlaps M2 (sell-side offers = audit features#1) — mine it when starting M2.
NEXT WORK (user-ordered, none started):
1. **M2 TRANSFER MARKET** — (a) AI clubs bid on YOUR players: a user-only daily tick (no-op unless offers pending — dynasty-safe like scout briefs) generating offers from clubs with role-need+budget+stature (reuse Team::score_scout_target + the reputation gate in Team.cpp); offers land as MAIL with Accept/Reject/Counter buttons (mail items need action support: extend MailItem w/ an offer id + add act:offeraccept/offerreject/offercounter verbs; on accept: transfer player via existing user-sell fields will_sell/user_transfer_bid on Team, budget credit, mail confirm). (b) SELL/LIST: act:listplayer:<id> flag boosts AI interest. (c) WINDOWS: transfers_open() hardcoded true — define preseason + mid-season windows + deadline-day (last window day → extra offer volume + drama mail); gate user signfa/buyplayer + AI acquisition paths.
2. **M4 STATS REWORK** — player modal Detail: per-YEAR summary rows (aggregate tourn_stats buckets by the "|<year>" suffix); click year → expand per-event lines for that year (grouping already correct: "<region> Split N", "Masters N", "Champions"). Tag events w/ region + tier. PLUS in-season tournament leaderboards (a Stats tab on brackets screen: aggregate tourn_stats across participants for the active event key).
3. **M5 CUPS/INTL PREP** — extend tourn_identity_key (Match.cpp:66-97) label shapes for "Cup <name>|year" + "Intl <name>|year"; cups = Tournament instances over invited teams (formats already exist); intl = transient national Teams (exporters must tolerate league_for()==null — division_label already falls back).
4. **Save v2 (when asked)**: serialize active tournaments (blocker: raw Player* accumulators in Tournament — see inventory in memory Inc-24); live per-round ASSISTS (add assister to RoundEvent, Match.cpp ~1986); per-round stat snapshots for live ADR.
KNOWN QUIRKS TO REMEMBER: --js strips double quotes (use no-arg temp hooks); kill vlrweb+msedgewebview2 +3s wait between captures; --cmd needs ~13-24s; headless = reduced-motion (live viewer lands on final — temp startLive() to shoot mid-play); build.bat lists sources EXPLICITLY (SaveGame.cpp already added to all 4 targets); saves live in cpp\build\saves\.

PROGRESS UPDATE 2 (2026-07-02, post-build): **M1 + M3 COMPLETE AND VERIFIED.** Build clean 1st try (SaveGame.cpp ~1890 lines compiled+linked; agent added it to build.bat CORE_SRC + all 4 link lines). VERIFIED captures: main menu renders (CSS TRAP FIXED: body.menuMode must be grid-template-columns:1fr — hiding the rail makes #view auto-place into a 0-width first column → black screen); **SAVE→LOAD ROUNDTRIP WORKS** (act:save:1 wrote 3.4MB slot_1.vlrs; act:load restored squad/form/news/standings/objectives/schedule-cursor perfectly); Continue enables when saves exist; team-talk chips on matchday w/ correct underdog "fit" hint (Fire-up dashed-gold when OVR lower); wrap-up card (Series MVP + 4 derived awards + decider storyline). BUGFIX during verify: team-talk wiring used undefined `hero` var → ReferenceError killed squad/form/news rendering ($("#cHero") fix). Smoke s12 pending at write time. NOTE: --js gotcha reconfirmed (double quotes stripped → use no-arg hooks). Saves dir: cpp\build\saves\ (next to exe).
PROGRESS (2026-07-02): M1a start-maximized DONE (vlrweb ShowWindow SW_SHOWMAXIMIZED; --shot keeps 1480x940). M1b main menu DONE in app.html (showMainMenu client-rendered title screen; boot()→mainmenu when no hash; body.menuMode hides rail; Continue/New Career/Save/Load/Settings; slot panels; Settings = reduced-motion + autosave via localStorage vlr_reduced/vlr_autosave; `reduced` now `let` w/ override; rail "New Career" button REPLACED by "Menu"→showMainMenu; worldActive tracked in onState; menuMode stripped on any world push). M1d bridge DONE in vlrweb (saves_dir/slot_path <exe>\saves\slot_<s>.vlrs; post_saves_list → {screen:"saves",slots[1,2,3,auto]} via SaveGame::peek; get:saves; act:save:<slot>:<screen> w/ "saves"=refresh-panel + "quiet"=autosave-silent; act:load:<slot>:<screen> clears view transients + lands dashboard; #include "SaveGame.h"; app advance() posts act:save:auto:quiet when autosaveOn). M1c SaveGame.cpp DELEGATED to an implementation agent (full field blueprint from the completed save-inventory; v1 gates save when active_tournaments non-empty; skips pro/solo_match_history; restores rng AFTER entity construction; tier-alias + entity-id-counter + name-registry restore) — INTEGRATE+BUILD when it lands; check build.bat needs SaveGame.cpp added. M3a live K/D DONE (exporter emits per-round kills[]{k_id,k,v_id,v,side,t1_alive,t2_alive} from RoundLog.events — RoundEvent has killer/victim Player*; viewer tallies cumulatively per playhead onto .mvK/.mvD cells by data-pid; assists stay match-final — engine doesn't attribute per-kill assists; RoundEvent would need an assister field). M3b post-match wrap-up DONE (client-side seriesAgg over per-map boards → .mvWrapCard: gold Series-MVP card + derived awards [most kills/top rating/opening duels/clutch] + decider storyline; hidden while live). REMAINING: M1c integrate+verify, M3c team talk, M2 transfer market, M4 stats rework, M5 cups/intl prep.
**M1. SAVE/LOAD + MAIN MENU + SETTINGS + MULTI-SLOT + START MAXIMIZED** (flagship, do first)
   - Start maximized: vlrweb WinMain → ShowWindow(SW_SHOWMAXIMIZED). Trivial.
   - Main menu: a title screen (app.html screen "mainmenu") shown at boot BEFORE any world screen: Continue (latest save), New Career (→ existing newgame wizard), Load Game (slot list), Settings. Boot flow change: vlrweb no longer builds the default 0xC0FFEE world for normal play — boot to mainmenu; --shot keeps the deterministic default world for verification (GATE: only build_world() when a --shot/test flag is present, else lazy).
   - Save format: hand-rolled BINARY chunked format (we have NO json reader — only JsonWriter). New files cpp/src/SaveGame.cpp + include/SaveGame.h: BinWriter/BinReader (POD, string, vector, map templates), magic "VLRS", version int (v1), then chunks. Serialize by STABLE ID + global pools: all players once (by id, full state incl. attributes/contract/histories/tourn_stats/reputation/mastery/awards), all staff once (coach/scout/analyst by id), teams (identity + roster as player-id list + staff ids + finances + trophy_case + history), leagues/tier pyramid (region→tier→team names + schedules + current_week), solo_qs ladders (player-id lists per region), GameManager scalars (year, day_in_year, phase_idx, preseason, budget-related, user_team by name, user_auto_manage, world_difficulty_, sponsor state, scout credits/assignments/briefs/accuracy map, mailbox, favorites/watchlist), rng() engine state (serialize the mt19937/engine state so the sim continues deterministically), name caches (g_used_names/g_taken_team_names/g_used_handles + cursors) to keep uniqueness after load.
   - ACTIVE TOURNAMENTS: v1 policy — serialize Tournament state if feasible; if too hairy, BLOCK save during active tournament days with a toast ("finish this tournament round day") — but tournaments span many days, so prefer serialization. INVENTORY AGENT RESULT REQUIRED (launched 2026-07-02; see memory/transcript).
   - Slots: saves/<slot>.vlrs (3+ slots + autosave). vlrweb verbs: act:save:<slot>, act:load:<slot>, get:saves (slot metadata: club, year, day, timestamp — needs a small header chunk readable without full load). Settings (modest v1): stored in saves/settings.json (written by host): reduced-motion override, autosave on/off. UI in main menu.
**M2. TRANSFER MARKET ALIVE**: (a) AI clubs bid on YOUR players — periodic tick (user-only, dynasty-safe empty-fast-path) where AI clubs w/ need+budget+stature (reuse score_scout_target + reputation gate) generate offers → mailbox with Accept/Reject/Counter actions (mail needs action buttons; wire act:offeraccept:<offerId> etc.); (b) SELL/list a player (act:listplayer:<id> → flag; listed players get boosted AI interest); (c) TRANSFER WINDOWS: transfers_open() currently hardcoded true — define windows (e.g. preseason + mid-season break; deadline-day = last day w/ extra offer volume + mail drama). Gate user signfa/buyplayer + AI acquisition paths per windows.
**M3. MATCH-DAY TRIO**: (a) LIVE per-player stats in the live viewer — Match.cpp already has per-round RoundLog.events (kill events with actor?) — INVENTORY: RoundLog::events RoundEvent carries killer/victim names? If yes, export per-round cumulative K/D per player (rounds[i].box[] snapshot or derive client-side from kill events) — derive CLIENT-SIDE from events if possible (no engine change); else add per-round snapshot accumulation in the recording path (user matches only). (b) POST-MATCH WRAP-UP: MVP card + match awards + storyline banner on the series end state (data exists: mvp_name, storyline, per-map stats). (c) PRE-MATCH TEAM TALK: before a user matchday, a choice card (e.g. Calm / Fire-up / Demand focus) → small deterministic morale/prep tilt for that match (user-only field consumed by the user match path; dynasty-safe default neutral).
**M4. STATS UI REWORK (player profile)**: Detail tab "By Tournament" → group BY YEAR first: a per-year summary row (aggregate of that year's buckets); CLICK a year → expands into that year's per-event lines (Split 1/Split 2/Split 3 [region-tagged], each Masters separate, Champions separate — grouping already matches tourn_identity_key). Sort/tag events per REGION and per TIER (tier of the event = the league tier the player's team was in that year — approximate v1: tag domestic splits with the team's CURRENT tier; internationals are tier-agnostic). ALSO: a tournament-stats view DURING the season for any tournament (e.g. a "Stats" tab on brackets screen: per-tournament leaderboards — kills/rating/ADR across that event's participants, via tourn_stats key match).
**M5. ARCHITECTURE PREP — OFF-SEASON CUPS + INTERNATIONAL PLAY**: keep tourn_identity_key extensible: reserve label shapes "Cup <name>|<year>" and "Intl <name>|<year>"; make the phase calendar data-driven enough that new phases (OFFSEASON CUP between AWARDS and OFFSEASON; INTL windows) can be inserted without breaking kPhases consumers (currently index-based — document consumers); Tournament class already supports arbitrary team sets + formats (SingleElim/DoubleElim/GroupThenPlayoffs) so cups = new Tournament instances over invited teams; national teams for international play = transient Team objects with country rosters (NO league membership — verify exporters don't assume league_for(t) non-null... division_label falls back safely).

## G — QoL / FEATURE / UI AUDIT (2026-06-28, prioritized; next work queue)
**Tier 1 — high impact, small/medium effort**
1. **SAVE/LOAD** — still the #1 gap (user holding off until done right). Everything else is quality-of-life next to this.
2. **Transfer offers for YOUR players** — AI never bids on user players; sell path missing (will_sell/sell_threshold_k/user_transfer_bid exist in engine). Pairs with mail: "Club X bids $Y for Z — accept/reject/counter."
3. **Scouting-brief reveal notifications** — briefs reveal silently; toast + mail "Your scout flagged <player> (matches 'U23 Duelists')" with click-through to the modal.
4. **Roster table column sorting** (click OVR/AGE/RAT/K/D/CONTRACT headers) + same for standings.
5. **Player comparison** — pick 2 players → side-by-side attribute deltas (scout target vs your starter in that role).
6. **Calendar links** — fixture rows → opponent teamprofile; overlay phase markers (Regionals/Masters/Champions weeks).
7. **Mail category filters + unread-only toggle** (categories already exported).
8. **Role picker on market quick-sign** (modal Sign tab has it; market SIGN button is natural-role only).
9. **Next-match card everywhere** — "Continue" advances blind; surface "next: vs X in Nd" on non-dashboard screens (thin strip under rail?).
10. **Records/HoF rows → player modal** (verify records screen wiring; some lists lack ids).
**Tier 2 — medium**
11. **Live per-player K/D during live match playback** (export per-round stat snapshots; known limitation from Inc 18).
12. **Bracket screen: click team → profile; show per-series map scores** (bracket export has best_of now).
13. **Training focus** — expose the dev system (coach dev_chance_mult exists): pick a focus attribute per player.
14. **Year-end season review screen** — awards recap, your arc, league story (data all exists at AWARDS phase).
15. **League-wide transfer news feed** — engine logs AI moves; surface a "Around the league" mail/news digest.
16. **Multi-year tournament stats filter** — tourn_stats accumulates across years; add a year dropdown to the By Tournament table.
17. **Finance projections detail** — projected income vs committed payroll table (fields exist: projected_income_k, committed_payroll_k, net_transfer_k).
**Tier 3 — flagship**
18. **Transfer windows + deadline day** (transfers_open() hardcoded true).
19. **Contract clauses** (release clause, wage escalators) — engine fields partially exist.
20. **Board confidence + sack risk** — objectives exist; add a confidence meter + firing consequence.
21. **Playoffs hype: pre-match narrative card** (rivalry/h2h data exists via narrative tracking).
22. **Achievements/trophy cabinet UI** (trophy_case exists on Team).

## Prior standing feedback still open (from memory, pre-this-batch)
- Post-match wrap-up polish; close transfer market (AI bids on user FAs + user sell); transfer WINDOWS; contract clauses; Scouting&Match-Prep batch; SAVE/LOAD (#1 gap).
- Live match viewer per-player K/D/A shows FINAL during live playback (per-round stat snapshots not exported) — known limitation; user may want live per-player stats.

## Implementation seams (from 2026-06-28 investigation — use these)
- **#11 Tournament stats (EASY, data exists):** `Player::tourn_stats` (unordered_map<string,TournStatLine>, Player.h:536-552) is ALREADY populated per-tournament by Match.cpp:2362 via `tourn_identity_key()` (Match.cpp:66-97) which collapses exactly per the user's grouping (Regionals+Stage of a split → "<region> Split N|year"; Masters N + Champions separate). Just EXPORT it in export_player_profile_to_json (after career block ~DashboardExport.cpp:2156): emit `tournament_stats` map {maps,matches,kills,deaths,assists,rating=rating_total/maps,adr=damage/rounds,hs%,kast}. Add a tournament dropdown in the player modal (desktop GUI already does this at gui_main.cpp:4185-4284 as a pattern).
- **#9/#10 Standings region+tier (MEDIUM):** `tier_leagues_[region][tier-1]` holds all tiers (GameManager.h:252); helpers `league_at(region,tier)`, `tier_of(t)`, `tier_count(region)`. Parameterize `export_competition_to_json(gm, region="", tier=0)` (DashboardExport.cpp:904; replace `sorted_league(gm,user)` with `league_at(target_region,target_tier)->teams()` sorted; fix region at :923/:927 + schedule filter :958). Bridge via a command like `get:competition:<region>:<tier>` (NOT http — the agent's fetch() idea is wrong). Add region/tier `<select>` switcher in renderCompetition (app.html; nav is `go('competition')`).
- **#4 Staff profiles (MEDIUM):** Coach/Scout/Analyst each have stable `id` + full attrs (Coach.h:43 tactical/development/leadership/experience; Scout.h:30 judgement/network/projection; Analyst.h:34 tactical_read/opponent_insight/prep; all have personality/reputation/career/contract). Add `find_staff_by_id` (scan free_+retired_+team head_* pools) + `export_staff_profile_to_json(gm,id)` + `nav:staffmodal:<id>` (vlrweb g_staffId) + a staff modal in app.html (mirror playermodal). Wire clicks from staff cards (roster strip + recruitment Staff subtab + market).
- **#5 Staff FA gen at init (SMALL, dynasty-check):** free_coaches_/free_scouts_/free_analysts_ are EMPTY at world init (GameManager.cpp:65-251 only assigns one per team); year-end run_*_market refills to 30. Add generation in initialize_world (after ~line 217): loop generate_coach/scout/analyst(region) into the free pools (~10-15/region). RNG-stream change → run smoke to confirm invariant tests still pass.
- **#13 Reputation (BIG, dynasty-sensitive — own turn):** Player has NO reputation. Plan: add `int reputation=<seed>` to Player.h (~:400) seeded in generate_player (scale to OVR/potential/tier so it starts realistic, not flat 5000); update slowly in save_history_and_progress (Player.cpp:1411; +from [W]/[M]/[T] trophies, MVPs, season rating; baseline decay toward a tier anchor). GATE interest in `Team::score_scout_target` (Team.cpp:990-1084, after affordability): compute buyer tier from wealth_tier+prestige and reject if player reputation >> buyer tier (a tier-3 club can't pursue a tier-1-rep player unless the player regressed). Influence gen_contract wage (Player.cpp:322) + get_transfer_value + award noms. Export in interest/profile JSON. CAUTION: changes AI transfer stream → verify smoke [85] (score_scout_target-grounded interest) + RNG-sensitive [27]/[44] still pass; may need to keep reputation OUT of the value/wage math on the dynasty path or accept invariant tests re-pass.
- **#12 Interest tab scroll (SMALL):** admirers already capped at 8 in export_player_interest_to_json (DashboardExport.cpp:~3138); make the interest list container scrollable in the player-modal interest pane (app.html). (Real fix for "too many teams" = #13 reputation gating.)
- **#6 Role on sign (SMALL):** do_signfa already takes role_idx (`act:signfa:<id>:<years>:<role>:<bonus>:<screen>`); add a role picker to the FA sign form in app.html.
- **#7 God-mode edit name (SMALL):** add `act:setname:<id>:<name>:<screen>` verb (do_setname on user roster only, like do_setattr) + a name field in the roster god-mode editor.

## Notes / decisions
- Increment 18 (last turn) fixed: staff-clip (VERTICAL, roster scroll — INSUFFICIENT, see #2), region label (hardcoded "Americas League"), collision-proof ownership (player_on_user_team by roster id + test [84b]), brief skip roster (WRONG — reverting per #1), BO3/BO5 series display + LIVE match viewer (renderMatch rewrite + export_match_to_json series-aware).
